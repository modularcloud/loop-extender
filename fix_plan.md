# Implementation Plan for loopx Test Harness

**Status: ADR-0004 §6.1 (project-root cwd + LOOPX_WORKFLOW_DIR injection), §7.2 (first-trigger-wins surfaced-outcome under racing terminals — CLI signal-handler fix + new `firstObservedRef` shared trigger-tracking slot threaded between run.ts wrapper and runLoop), §7.4 (LOOPX_TMPDIR creation/injection/cleanup + parent-snapshot timing across all four injection tiers + run-setup non-reaping + renamed-away ENOENT silence + symlink-replacement cleanup-rule-2 surface-parity + regular-file-replacement cleanup-rule-3 surface-parity + mismatched-directory cleanup-rule-5 surface-parity + nested-symlink-recursive-walk cleanup-rule-4 surface-parity + FIFO/socket/hard-link cleanup-rule-3 generic-dispatch coverage + lstat-fail / symlink-unlink-fail / recursive-remove-fail cleanup-failure surface-parity + cleanup-warning-does-not-mask-script-error surface-parity + cleanup-fault-warning-does-not-mask-script-error surface-parity + cleanup-warning-does-not-mask signal/abort terminal surface-parity + cleanup-fault-warning-does-not-mask-CLI-signal terminal surface (T-TMP-42c — closes the cleanup-failure × signal × CLI diagonal) + cleanup-fault-warning-does-not-mask-abort-terminal surface (T-TMP-42b — closes the cleanup-failure × abort × programmatic surfaces diagonal) + cleanup idempotence under racing SIGINT→SIGTERM at the cleanup-start window for rule-3 and rule-5 branches, and (T-TMP-38f cleanup idempotence under racing SIGTERM→SIGINT — inverted-order parity), and (T-TMP-38a first-observed-wins under racing abort → .return() during cleanup-start on the run() programmatic surface), and (T-TMP-38a2 first-observed-wins inverted-order parity — racing .return() → abort during cleanup-start on the run() programmatic surface), and (T-TMP-38b/T-TMP-38b-run first-observed-wins under racing non-zero-script-exit → abort during cleanup-start, runPromise() and run() surfaces — test-only iteration; existing run.ts wrapper + runLoop try/finally already conformed), and (T-TMP-38c first-observed-wins under racing abort → .throw() during cleanup-start on the run() surface — the .throw()-axis companion to T-TMP-38a, completing the abort-first × consumer-cancellation matrix at the cleanup-start window — test-only iteration), and (T-TMP-38c2 first-observed-wins inverted-order parity — racing .throw() → abort during the new `consumer-throw-observed` window seam on the run() surface — implementation iteration: introduced shared `test-seams.ts` helper for the §1.4 `LOOPX_TEST_TERMINAL_TRIGGER_PAUSE` mechanic and added the `consumer-throw-observed` window value at the wrapper.throw entry of run.ts), and (T-TMP-38d3 first-observed-wins under racing post-final-yield .return() → abort during the new `consumer-return-observed` window seam on the run() surface, parameterized over `(stop)` and `(maxIterations)` final-yield triggers — wired the `consumer-return-observed` window value at the wrapper.return entry of run.ts, mirroring the `consumer-throw-observed` placement), and (T-TMP-38d2 first-observed-wins under racing post-final-yield .throw() → abort during the `consumer-throw-observed` window seam on the run() surface, parameterized over `(stop)` and `(maxIterations)` final-yield triggers — test-only iteration; existing seam wiring at wrapper.throw line 461 naturally handles the post-final-yield × abort-not-observed branch through the same `!abortObservedFirst` gate that fires on mid-loop .throw()), and (T-TMP-38b2/T-TMP-38b2-run first-observed-wins under racing abort → non-zero-script-exit during the new `abort-listener` window seam on the runPromise() and run() surfaces — implementation iteration: introduced the `firstObservedRef` shared slot, wired the `abort-listener` seam at the user-signal listener (which now async-defers `internalAc.abort()` when the seam is active to keep the active child alive during the bounded pause, qualifying §1.4 form (a)), and refactored every `internalAc.signal.aborted` first-observed check in run.ts (.return / .throw / asyncDispose / wrapper.next() catch) to consult `firstObservedRef.trigger === "abort"` instead so the seam-deferred dispatch does not leave first-observed determination wedged on a stale clock; runLoop now pins `"iteration"` before each iteration-level throw so a racing abort observed later cannot reclassify the outcome as abort)), §9.5 (RunOptions.env tier-2 env merging), §9.3 (abort-after-final-yield carve-out), §9.2 (LOOPX_TMPDIR async creation under runPromise()), AND §9.2 (process.env eager snapshot under runPromise()) are now IMPLEMENTED. Recent (this iteration): introduced new SPEC infrastructure for the §7.2 first-observed-wins contract that decouples first-observed determination from `internalAc.signal.aborted` (which the new `abort-listener` seam delays). Specifically: added `FirstObservedRef` shared-state interface in `loop.ts`, wired a single `firstObservedRef = { trigger: null }` slot per run() call in `run.ts` that is pinned to `"abort"` by the user-signal listener at entry, `"iteration"` by `runLoop` before each iteration-level throw (script-failure / goto errors), or `"consumer"` by the wrapper's `.return()` / `.throw()` / `[Symbol.asyncDispose]` when consumer cancellation acts as the first observed trigger. The user-signal listener now async-defers `internalAc.abort()` when `NODE_ENV=test` AND `LOOPX_TEST_TERMINAL_TRIGGER_PAUSE === "abort-listener"`, keeping the active child alive (TEST-SPEC §1.4 form (a)) while the bounded ~3s pause runs and a parent harness can race a second terminal trigger into the still-paused dispatch. The `wrapper.next()` catch now surfaces `makeAbortError(...)` whenever `firstObservedRef.trigger === "abort"`, regardless of what error reached it from runLoop — this is what produces the abort-error surfacing under T-TMP-38b2 / T-TMP-38b2-run when the script's SIGUSR1-driven `exit 1` reached runLoop first as a script-failure but abort had already been pinned as first-observed by the listener. Authored T-TMP-38b2 (runPromise) and T-TMP-38b2-run (run) — bash fixture installs a `trap 'exit 1' USR1`, writes its own PID to a marker, replaces the tmpdir with the rule-3 regular-file replacement, then `sleep 30 & wait`s. Driver code calls `ac.abort()` first (pins firstObservedRef to "abort", schedules the abort-listener pause), then polls the parent-observable pause marker, then sends `SIGUSR1` to the script's process group (`process.kill(-pid, "SIGUSR1")`) so the racing iteration error fires while loopx is still paused. Asserts: (a) errName === "AbortError" and errMessage does NOT match `/exited with code/` (load-bearing — a buggy wrapper.next() catch that surfaced the iteration error directly without consulting firstObservedRef would shift this), (b) exactly one cleanup warning (per-run cardinality holds across racing triggers), (c) rule-3 regular-file replacement at the recorded path survives. 1 ID × 2 surfaces × 2 runtimes = 4 tests. tmpdir.test.ts: 541/541 (was 537, +4). Adjacent suites: programmatic-api + env-vars + wfdir + loop-state + execution (635/635) zero regressions; signals (9/9) zero regressions; unit + harness (158/158) zero regressions. Most-recent iteration (T-TMP-38d / T-TMP-38d4): authored 8 tests (2 IDs × 2 final-yield variants × 2 runtimes) closing the post-final-yield abort-first × consumer-cancellation matrix at the abort-listener seam. Test-only iteration; the wrapper's existing post-final-yield abort branch (`firstObservedRef.trigger === "abort"` + `surfacePostFinalYieldAbort`) already conformed for both `.throw()` and `.return()` consumer methods. Bash fixture writes rule-3 regular-file replacement + emits final yield (`{"stop":true}` for variant (stop), raw `{}` for variant (maxIterations)) + exits 0; driver `await gen.next()`s the final yield, calls `ac.abort()` FIRST (pins firstObservedRef + schedules abort-listener seam pause), polls pause marker, then races `gen.throw()` (T-TMP-38d) or `gen.return()` (T-TMP-38d4) during the bounded pause. Asserts: (a) errName === "AbortError" (the abort outcome is the surfaced terminal outcome — racing consumer cancellation does NOT displace it; for T-TMP-38d the consumer-supplied "test-throw" message also does NOT appear; for T-TMP-38d4 returnDone is false — no clean settlement), (b) exactly one cleanup warning, (c) rule-3 file persists with content `regular-file-replacement`. tmpdir.test.ts: 561/561 (was 553, +8). Adjacent suites: zero regressions across programmatic-api + env-vars + wfdir + loop-state + execution (635/635); signals (9/9); unit + harness + typecheck (172/172). Most-recent iteration (T-API-66/66a/66b/66c/66d/66e): authored 12 tests (6 IDs × 2 runtimes) pinning the SPEC §9.3 abort-after-final-yield contract on the API-surface OUTCOME axis — error identity (AbortError) and rejection (vs silent `{ done: true }` settlement) for all three settle-triggering interactions (`.next()`, `.return()`, `.throw()`) × both final-yield triggers (maxIterations-reached, stop:true). Test-only iteration; the wrapper's existing postFinalYield + firstObservedRef + surfacePostFinalYieldAbort branches at run.ts:398/451/519 already conformed. Complements the cleanup-ORDERING-axis tests in tmpdir.test.ts (T-TMP-23/24a/24c/24d/24e/24f/24g) which assert tmpdir is removed before abort surfaces — T-API-66 series asserts the abort error surfaces in the first place. programmatic-api.test.ts: 370/370 (was 358, +12). Adjacent suites: zero regressions across tmpdir (561), env-vars (96), wfdir (40), signals (9), loop-state (96), execution (45), unit (143), harness (15), typecheck OK. Most-recent iteration (T-TMP-38e / T-TMP-38e-run): wired two new TEST-SPEC §1.4 seam windows in `execution.ts` (`child-spawn-attempt` BEFORE `spawn()` and `child-spawn-failure` AFTER catching the spawn rejection), threaded `firstObservedRef` through `ExecOptions` so spawn failures pin `trigger = "iteration"` before the seam pause, and gated `runLoop`'s `abortPromise` to NOT reject when `firstObservedRef.trigger === "iteration"` so a racing abort during a spawn-failure pause cannot displace the iteration outcome via `Promise.race`. Authored 8 tests (2 IDs × 2 variants × 2 runtimes) closing the spawn-failure × abort cleanup-idempotence × warning-cardinality matrix on programmatic surfaces. tmpdir.test.ts: 569/569 (was 561, +8). Adjacent suites: zero regressions across programmatic-api + env-vars + wfdir + loop-state + execution (635/635 e2e), signals (9/9), unit + harness (158/158). Most-recent iteration (T-API-50 series — RunOptions.env basic injection): authored 14 tests (7 IDs × 2 runtimes) pinning the SPEC §9.5 / §8.3 RunOptions.env contract on the API surface — basic injection (T-API-50, runPromise), across iterations (T-API-50a, run), across intra-workflow goto (T-API-50b), across cross-workflow goto (T-API-50c), on loop reset (T-API-50d), empty-string value preservation (T-API-50e — `${MYVAR+x}` set-vs-empty distinction), and 11-axis tricky-non-NUL value preservation across both surfaces (T-API-50h: whitespace, `=`, `#`, double/single quotes, backslash, UTF-8 + emoji, LF, CRLF, TAB, whitespace-only — bytes-unchanged byte-for-byte through both runPromise and run). Test-only iteration; the §9.5 RunOptions.env tier-2 env merging implementation already conformed. programmatic-api.test.ts: 384/384 (was 370, +14). Adjacent suites: zero regressions across env-vars + wfdir + loop-state + execution (277/277). Most-recent iteration (T-API-51 series — RunOptions.env precedence chain): authored 14 tests (7 IDs × 2 runtimes) pinning the SPEC §8.3 / §9.5 / §13 / §7.4 precedence contract on the API surface across each precedence boundary — tier-2 vs tier-1 silent override of all five script-protocol-protected `LOOPX_*` names with during-run `LOOPX_TMPDIR` stat for real-vs-substituted-string disambiguation × runPromise + run (T-API-51a / 51a2), tier-2 vs tier-3 local env file (T-API-51b), tier-2 vs tier-4 global env file under XDG_CONFIG_HOME (T-API-51c), tier-2 vs tier-5 inherited process.env (T-API-51d), and full chain assertion on both surfaces (T-API-51e / 51f). The 51a / 51a2 fixture observes each protocol name into per-name marker files, performs an in-script `[ -d "$LOOPX_TMPDIR" ]` stat (during execution — SPEC §7.4 cleanup runs after the script, so a post-run stat would observe absence), and asserts (a) marker values match real protocol values not user-supplied `/tmp/fake-*`, (b) during-run stat marker records `is-dir` (proves real loopx-created tmpdir, not substituted string), (c) stderr matches NO `<name>.*(override|overrid|ignored|warning|notice)` regex for any of the five names — confirming the SPEC §13 / §8.3 silent-override contract. Test-only iteration; the §9.5 precedence implementation in `run.ts` (mergeEnv tiers 5→4→3 + `snap.env` overlay tier 2 at run.ts:661-664) and `execution.ts` (protocol-var overlay tier 1 at execution.ts:179-185) already conformed. programmatic-api.test.ts: 398/398 (was 384, +14). Adjacent suites: zero regressions across env-vars + wfdir + loop-state + execution (277/277), tmpdir (569/569), signals (9/9), unit + harness (158/158), typecheck OK. Most-recent iteration (T-API-52 series — RunOptions.env snapshot semantics): authored 20 tests (10 IDs × 2 runtimes) pinning the SPEC §9.5 / §9.1 / §9.2 eager-shallow-copy contract on the API surface — outer-property value mutation post-call (T-API-52 runPromise, T-API-52b run for call-vs-first-next() distinction), outer-property key-addition post-call on both surfaces (T-API-52a run, T-API-52f runPromise), per-entry accessor-getter call-time-once + no-per-spawn-re-read on both surfaces (T-API-52c run with `Object.defineProperty`, T-API-52d runPromise across 3-iter), and proxy trap-call cardinality on both surfaces (T-API-52e ownKeys+get traps, T-API-52e2 descriptor-only and get-only single-trap variants). Test-only iteration; the existing `snapshotEnv` (run.ts:72-103) called synchronously inside `snapshotOptions` (run.ts:105-232) at the head of `runWithInternal` (run.ts:267) already conformed: `Object.keys()` enumerates own-enumerable string-keyed properties exactly once (which invokes proxy `ownKeys` once and the implementation-defined `getOwnPropertyDescriptor` count); each value `[[Get]]` runs once at call time; the resulting `Record<string, string>` is a fresh shallow copy detached from the original. `runPromise` synchronously calls `runWithInternal` before the `await Promise.resolve()` microtask boundary, preserving eager call-time semantics on the eager surface. programmatic-api.test.ts: 418/418 (was 398, +20). Adjacent suites: zero regressions across tmpdir (569/569), env-vars + wfdir + loop-state (232/232), signals (9/9), unit + harness (158/158), typecheck OK.**

## P0/P1 — RESOLVED

### ADR-0004 §6.1 (project-root cwd + LOOPX_WORKFLOW_DIR injection)

Implemented in `packages/loop-extender/src/execution.ts`:
1. Script execution cwd changed from `workflowDir` → `projectRoot` (SPEC §6.1 / ADR-0004 §3).
2. `LOOPX_WORKFLOW_DIR` is now injected into every spawned script's environment (SPEC §6.1 / §8.3 / §13).

Passing test groups:
- **T-WFDIR-01..14 (LOOPX_WORKFLOW_DIR injection)** — ALL 40/40 pass.
- **T-EXEC-01, T-EXEC-02, T-EXEC-16, T-EXEC-16b** — project-root cwd assertions.
- **T-API-07a, T-API-47b** — RunOptions.cwd controls script execution cwd.

### ADR-0004 §7.4 (LOOPX_TMPDIR creation / injection / cleanup)

Implemented in `packages/loop-extender/src/tmpdir.ts` and integrated via `loop.ts`, `execution.ts`, `run.ts`:

1. **Three-step creation** (SPEC §7.4): `mkdtemp(<parent>/loopx-)` → identity capture (`lstat` dev/inode) → mode 0700 secure (`chmodSync`).
2. **Five-case cleanup-safety dispatch** (SPEC §7.4): ENOENT no-op; symlink unlink (no-follow); non-directory leave-with-warning; identity-match recursive remove; identity-mismatch leave-with-warning.
3. **Cleanup idempotence** — `CleanupState { attempted, warned }` guarantees at most one cleanup attempt and at most one stderr warning per resource.
4. **Per-creation-failure handling** (SPEC §7.4 "does not mask the original creation error"): identity-capture-fail triggers single non-recursive `rmdir`; mode-secure-fail triggers full cleanup-safety routine; both throw the original creation error after cleanup.
5. **Test seams** (TEST-SPEC §1.4, NODE_ENV=test only): `LOOPX_TEST_TMPDIR_FAULT={identity-capture-fail, identity-capture-fail-rmdir-fail, mode-secure-fail}` and `LOOPX_TEST_CLEANUP_FAULT={lstat-fail, symlink-unlink-fail, recursive-remove-fail}`.
6. **Structured warning marker** — emitted under NODE_ENV=test as `LOOPX_TEST_CLEANUP_WARNING\t<payload>` for test-side detection.
7. **Integration**:
   - `runLoop` creates the tmpdir between SPEC §7.1 step 5 (version check) and step 6 (first child spawn). Wraps the loop body in `try/finally` so every terminal outcome triggers cleanup.
   - `executeScript` accepts `tmpdir: string` in ExecOptions, injects `LOOPX_TMPDIR` as a protocol-tier env var (overrides any user-supplied value).
   - `runPromise` captures `os.tmpdir()` eagerly at the call site (SPEC §9.2); `run()` and CLI use lazy capture inside `runLoop`.
8. **Lazy NODE_PATH shim** — `getLoopxShimDir()` is now invoked inside `executeScript` (was module-load). Prevents loopx from crashing when TMPDIR points at an unwritable parent before tmpdir-creation has had a chance to surface its own EACCES.

Passing test groups (tmpdir.test.ts):
- **T-TMP-01..09** (creation, scope, sharing across iterations / intra-workflow goto / cross-workflow goto / loop reset / persistence within run / concurrent runs across projects + same project CLI).
- **T-TMP-10/11/11a/11b** (no tmpdir under -n 0 / maxIterations: 0).
- **T-TMP-12** programmatic 26 sub-cases × 2 surfaces × 2 runtimes — pre-iteration failure modes ALL pass except the 6 RunOptions.env shape sub-cases (out of scope).
- **T-TMP-12-cli, T-TMP-12-cli-usage** (16 sub-cases × 2 runtimes).
- **T-TMP-12a/12b/12c** — unwritable-parent mkdtemp failure across CLI / run / runPromise × node + bun.
- **T-TMP-12d/12d2/12e/12e2/12e3** — 5 sub-cases × 3 surfaces × 2 runtimes (= 30 invocations) — LOOPX_TEST_TMPDIR_FAULT + LOOPX_TEST_CLEANUP_FAULT seam coverage. ALL PASS.
- **T-TMP-12f/12f2/12f3/12f4/12f5 + T-TMP-12g/12h** — 5 pkg-json variants × 3 surfaces × 2 runtimes (= 30 invocations) — version-check-before-tmpdir-creation ordering. ALL PASS.
- **T-TMP-13/13a/13b/14/14a/15/15a/15b** — cleanup on normal completion (stop / maxIterations / loop reset).
- **T-TMP-16/16a/16b/16c/16d/16e/16f/16g/16h/16i/16j** — goto-resolution-failure cleanup, mixed surfaces (= 30 invocations). ALL PASS.

Test-file cleanups previously landed:
- `writeCwdToFile` helper now uses `/bin/pwd -P` (per SPEC 6.1 — `$PWD` non-authoritative).
- §4.4 header comment in `execution.test.ts` rewritten to describe ADR-0004 cwd behavior.

### Test harness improvement (api-driver.ts)

`apps/tests/tests/helpers/api-driver.ts` now strips caller-supplied `extraEnv.TMPDIR` from the spawned child's env and bakes a `process.env.TMPDIR = <orig>` prefix into the driver code. This decouples tsx's eager `${TMPDIR}/tsx-${UID}` IPC dir from the loopx-perceived TMPDIR — tests can pass an unwritable TMPDIR without crashing tsx at module-load. loopx still observes the test's intended TMPDIR because every `os.tmpdir()` read in loopx is lazy (inside `runLoop`, `runPromise`'s eager-snapshot call, or pre-iteration in the CLI), all of which execute after the prefix.

### ADR-0004 §9.5 (RunOptions.env tier-2 env merging) — RESOLVED

Implemented in `packages/loop-extender/src/types.ts` and `packages/loop-extender/src/run.ts`:

1. **Public type** — added `env?: Record<string, string>` to `RunOptions` (types.ts).
2. **Option-snapshot machinery** (run.ts):
   - `snapshotOptions(options)` reads recognized fields once each via `[[Get]]` (signal first per SPEC §9.1, then cwd, envFile, env, maxIterations).
   - `snapshotEnv(envRaw)` validates shape (rejects null / array / function / non-object), enumerates own-enumerable string-keyed entries via `Object.keys` (which invokes Proxy `ownKeys` and `getOwnPropertyDescriptor` traps and propagates throws naturally), and reads each included value once via `[[Get]]` (which invokes accessor getters or Proxy `get` traps and propagates throws naturally).
   - `isAbortSignalCompatible(signal)` enforces SPEC §9.5 signal contract: `aborted` must be readable boolean, `addEventListener` must be callable.
3. **Eager cwd snapshot** — `cwd` snapshotted at call time (defaults to `process.cwd()` per SPEC §9.5); relative `cwd` resolved against `process.cwd()` once at call time.
4. **Signal wiring** — User signal → internal AbortController abort propagation now wired via `addEventListener` (replaces `AbortSignal.any` to support duck-typed signals); a throwing `addEventListener` is captured as a snapshot error.
5. **Lazy error surfacing** — Captured snapshot errors surface lazily via the standard pre-iteration error path on first `next()` (or as promise rejection from `runPromise`). Pre-first-`.return()` consumer-cancellation carve-out preserved (errors not thrown when consumer's first interaction is `.return()` / `.throw()`).
6. **Abort precedence** — SPEC §9.3 preserved: signal-aborted-at-call-time wins over snapshot errors.
7. **Merge order** (run.ts `runInternal`): `mergeEnv(globalEnv, localEnv)` (tiers 5→4→3) overlaid with `snap.env` (tier 2). Protocol-tier vars (LOOPX_*, tier 1) continue to overlay last in `executeScript` at execution.ts.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 282/282 PASS** — T-TMP-12 throwing-env-entry-getter, throwing-env-proxy-ownKeys, throwing-env-proxy-get sub-cases × 2 surfaces × 2 runtimes (16 tests); T-TMP-08a (concurrent runPromise) and T-TMP-08c (concurrent run generator) × 2 runtimes (4 tests); T-TMP-12 invalid-env-shape, invalid-env-value sub-cases (8 tests).
- **wfdir.test.ts T-WFDIR-08** — now passes for the right reason (user-supplied `LOOPX_WORKFLOW_DIR` plumbed via `RunOptions.env` and overridden by protocol-injected real workflow dir).
- Adjacent suites: zero regressions across programmatic-api (346), env-vars (96), execution (85), loop-state (96), wfdir (40), unit (143), harness (15).

### ADR-0004 §9.1 (.throw() consumer cancellation contract) — RESOLVED

The `.throw()` wrapper at `packages/loop-extender/src/run.ts` previously delegated directly to `gen.throw(err)`, leaving the active child unkilled and propagating the consumer-supplied error. SPEC §9.1 requires consumer-driven cancellation (including `.throw()`) to:
1. Terminate the active child process group (SIGTERM, then SIGKILL after 5s).
2. Ensure no further iterations start.
3. Produce silent clean completion (the consumer-supplied error is not surfaced) when no child is active.

The wrapper now mirrors `.return()`: calls `internalAc.abort()` to terminate the active child, then `gen.return(undefined)` to settle silently, swallowing any error so the caller sees `{ done: true, value: undefined }`. This is the no-active-child silent-completion contract from SPEC §9.1; for the active-child case the spec leaves the settlement form implementation-defined and we choose silent completion for symmetry with `.return()`.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 310/310 PASS** — added T-TMP-17..22f (28 new tests × 2 runtimes covering CLI signal cleanup, escalation × cleanup, programmatic abort cleanup, consumer cancellation × {.return(), .throw(), break} × {active-child, no-active-child, post-final-yield, stop:true-final-yield} matrix). The two escalation tests (T-TMP-18a/18b) carry `retry: 3` due to the 5-second grace-period wait, mirroring T-SIG-05 / T-SIG-05a.
- Adjacent suites: zero regressions across programmatic-api (346), env-vars (96), execution (85), loop-state (96), wfdir (40), signals (9), unit (143), harness (15). The `.throw()` change does not affect existing behavior because no prior test exercised `.throw()` on the wrapper.

### ADR-0004 §9.3 (abort-after-final-yield carve-out) — RESOLVED

The `run()` wrapper at `packages/loop-extender/src/run.ts` previously had no notion of post-final-yield state: after `yield output; return;` (or `yield output;` on the maxIterations boundary) the inner gen would return cleanly on the next interaction regardless of abort-signal state. SPEC §9.3 requires that an abort observed in the post-final-yield / pre-settlement window produces an abort error on the next interaction (`.next()` / `.return()` / `.throw()`), with first-observed-wins precedence so a prior consumer cancellation's silent-completion outcome is not displaced.

Implementation:
1. Wrapper now tracks `yieldCount`, `postFinalYield`, `settled` state across `.next()` / `.return()` / `.throw()`.
2. On each yield, if `output.stop === true` OR `yieldCount === snap.maxIterations`, set `postFinalYield = true`.
3. Before delegating to the inner gen, each method checks `postFinalYield && internalAc.signal.aborted && !returnCalled` — if true, drives `gen.return(undefined)` to run the inner `finally` (tmpdir cleanup), then throws `makeAbortError(snap.signal ?? internalAc.signal)`.
4. The `!returnCalled` guard implements SPEC §9.3 first-observed-wins: a prior consumer `.return()` / `.throw()` (which already set `returnCalled=true`) keeps its silent-completion outcome.
5. For `.throw()` post-final-yield-abort, the abort error displaces the consumer-supplied error (per SPEC §9.3).
6. `if (settled)` early-return added to each wrapper method for idempotent multi-call behavior.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 327/327 PASS** — added 17 new tests covering the §9.3 matrix:
  - **T-TMP-23**: post-final-yield → settlement (no abort) cleanup is single-shot and idempotent.
  - **T-TMP-24**: cleanup via full `for await` completion.
  - **T-TMP-24a/24c/24d**: abort after maxIterations-driven final yield + `.next()` / `.return()` / `.throw(consumerErr)` → cleanup runs before abort error (which displaces consumerErr in 24d).
  - **T-TMP-24e/24f/24g**: abort after stop:true-driven final yield + `.next()` / `.return()` / `.throw(consumerErr)` → same contract.
  - **T-TMP-24b**: external SIGKILL of loopx itself does NOT run tmpdir cleanup; tmpdir survives loopx death (CLI surface only, node only — Bun excluded because the harness's `runCLIWithSignal` plumbing relies on node spawn semantics). T-TMP-24b also surfaced a test-plumbing detail: when loopx is SIGKILLed its detached child shell holds an inherited stderr write-end, so `'close'` won't fire until the child also dies; the test now SIGKILLs the orphan child after observing the tmpdir-survival post-condition, then awaits result.
- Adjacent suites: zero regressions across programmatic-api (346), env-vars (96), execution (85), loop-state (96), wfdir (40), signals (9), unit (143), harness (15).

### ADR-0004 §9.2 (LOOPX_TMPDIR async creation under runPromise) — RESOLVED

The `runPromise()` body at `packages/loop-extender/src/run.ts` previously created `LOOPX_TMPDIR` synchronously at the call site, violating SPEC §9.2: "LOOPX_TMPDIR itself is created asynchronously after return, during the same pre-iteration sequence used by the CLI and run()." Authoring T-TMP-27a (which snapshots the parent directory's `loopx-*` entries synchronously between `runPromise()` returning and `await p`) caught this: `betweenSync` contained a freshly-created `loopx-*` entry that should not have existed yet.

Root cause: the `for await (const output of gen)` statement synchronously evaluates `gen[Symbol.asyncIterator]()` and the wrapper's `next()`, which synchronously calls the inner generator's `next()`, which synchronously runs `runInternal` + `runLoop` bodies up to the first internal `await` — invoking `createTmpdir` before `runPromise()` returned.

Fix: insert `await Promise.resolve()` between the sync option/parent capture and the `for await` loop. The synchronous `runWithInternal()` call is preserved before the microtask boundary so `snapshotOptions()` and the cwd default stay eager (T-API-14c, T-API-14d still pass). After the microtask, the for-await body runs and triggers the deferred `createTmpdir`.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 389/389 PASS** — added 62 tests covering "Tmpdir Parent Snapshot Timing" (T-TMP-25/25a/25b/26/26-temp/26-tmp/27/27-temp/27-tmp/27a/28/28a/28b/28c/28d/28e/28f/28g/28h/29/29a/29b/29c/29d/29e/29f/29g/29h/29i/29j/29k × 2 runtimes). T-TMP-27a specifically verifies the SPEC §9.2 async-creation contract; T-TMP-27/27-temp/27-tmp verify eager-parent capture; T-TMP-26/26-temp/26-tmp verify lazy-parent capture under `run()`; T-TMP-28/28a-h verify global-env-file-tier × {TMPDIR,TEMP,TMP} × {CLI, runPromise, run} contract; T-TMP-29/29b/29c verify `RunOptions.env` tier × {TMPDIR,TEMP,TMP}; T-TMP-29a/29d/29e verify CLI `-e` tier; T-TMP-29f-h verify `runPromise({envFile})` tier; T-TMP-29i-k verify `run({envFile})` tier.
- **Test infrastructure added**:
  - `getRuntimeOsTmpdir(runtime, envOverrides)` — spawns a child Node/Bun process with the given env and returns its `os.tmpdir()` value, used by all `TEMP` / `TMP` variant tests for the runtime-aware expected-parent assertion (since on POSIX runtimes only `TMPDIR` is consulted).
  - `withInheritedTmpdirEnv(overrides, body)` — snapshots and restores `process.env.TMPDIR/TEMP/TMP` around a test body.
  - `makeTestParent(label)` — creates a writable test-isolated parent directory under the system tmpdir, registered for cleanup.
  - `buildEnvObserveScript(observe[])` — bash fixture template that observes `LOOPX_TMPDIR` plus `TMPDIR`/`TEMP`/`TMP` into separate marker files via uniquely-named `OBS_*_PATH` env vars to avoid shadowing the variable being observed.
- Adjacent suites: zero regressions across programmatic-api (346), env-vars (96), execution (85), loop-state (96), wfdir (40), signals (9), unit (143), harness (15).

### ADR-0004 §9.2 (process.env eager snapshot under runPromise) — RESOLVED

The `runPromise()` body at `packages/loop-extender/src/run.ts` previously snapshotted only the tmpdir parent eagerly; the inherited `process.env` and global env-file path (`XDG_CONFIG_HOME` / `HOME`-derived) were still read lazily inside `runInternal` after the microtask boundary, violating SPEC §9.2: "Under runPromise(), the inherited process.env snapshot is eager — captured synchronously at the runPromise() call site, before runPromise() returns. Mutations to process.env after runPromise() returns are not observed."

Implementation:
1. **`packages/loop-extender/src/env.ts`**:
   - `getGlobalEnvPath(envSnapshot?)` accepts optional snapshot; when provided, reads `XDG_CONFIG_HOME` / `HOME` from snapshot rather than live `process.env`.
   - `loadGlobalEnv(envPath?)` accepts optional pre-resolved path verbatim.
   - `mergeEnv(globalEnv, localEnv, inheritedEnv?)` spreads the inherited snapshot when provided, falling back to `process.env` for backward compatibility (preserves `run()` lazy semantics).
2. **`packages/loop-extender/src/run.ts`**: `InternalRunOptions` extended with `inheritedEnv?` and `globalEnvPath?`, threaded through `runWithInternal()` / `runInternal()`. `runPromise()` now captures three eager snapshots before the `await Promise.resolve()`: `eagerTmpdirParent` (existing), `eagerInheritedEnv = { ...process.env }` (new), `eagerGlobalEnvPath = getGlobalEnvPath(eagerInheritedEnv)` (new).

Now-passing tests (this iteration):
- **programmatic-api: 370 PASS** (was 358; +12 new tests under "SPEC: Inherited Env Snapshot Timing" — T-API-71/71a/71b/72/72a/72b × 2 runtimes). T-API-71/71a/71b verify `run()` lazy semantics (mutation between call and first `next()` observed; mid-run mutation frozen at first `next()`; XDG_CONFIG_HOME mutation redirects global env file lookup). T-API-72/72a/72b verify `runPromise()` eager semantics (mutation after return not observed; mid-run mutation not observed across iterations; XDG_CONFIG_HOME mutation does not redirect global env file lookup).
- Adjacent suites: zero regressions (tmpdir 389, env-vars 96, wfdir 40).

### ADR-0004 §7.4 (lstat-fail / symlink-unlink-fail / recursive-remove-fail cleanup-failure surface-parity) — RESOLVED

SPEC §7.4 final paragraph: "If the top-level `lstat` fails for any reason other than ENOENT, the `unlink` in case 2 fails, or the recursive removal in case 4 fails, loopx emits a single stderr warning and makes no further changes." These three failure branches are specified normatively. Black-box reproduction is unreliable on a same-user-owned directory, so coverage is via the TEST-SPEC §1.4 `LOOPX_TEST_CLEANUP_FAULT={lstat-fail, symlink-unlink-fail, recursive-remove-fail}` seam (gated on NODE_ENV=test). The seam was already implemented in `packages/loop-extender/src/tmpdir.ts` at three failure-injection points: top-of-`cleanupTmpdir` (lines 184-189) for lstat-fail, the rule-2 `unlinkSync` (line 210) for symlink-unlink-fail, and the rule-4 `rmSync` (line 250) for recursive-remove-fail. Each emits via `emitCleanupWarning` which deduplicates through `CleanupState.warned` (single warning per resource). T-TMP-40 / T-TMP-41 / T-TMP-42 pin this contract on the **success terminal** across all three execution surfaces — a buggy implementation that wired one stream of "warnings do not affect outcome" handling for the CLI exit-code path and a separate stream for the API rejection path could pass the CLI variant and fail the API variants.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 453/453 PASS** — added 18 tests (9 IDs × 2 runtimes):
  - **T-TMP-40 (CLI) / T-TMP-40a (runPromise) / T-TMP-40b (run)**: `LOOPX_TEST_CLEANUP_FAULT=lstat-fail`. Fixture observes LOOPX_TMPDIR into an external marker and emits stop:true (no path manipulation needed; the seam fires on the top-level `lstat`). Asserts (a) success terminal outcome (CLI exit 0 / promise resolves with one Output / generator settles cleanly with one yield), (b) path observed still exists (cleanup aborted at the seam before any file operations), (c) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line on stderr.
  - **T-TMP-41 (CLI) / T-TMP-41a (runPromise) / T-TMP-41b (run)**: `LOOPX_TEST_CLEANUP_FAULT=symlink-unlink-fail`. Fixture observes LOOPX_TMPDIR, replaces it with `ln -s /tmp` symlink so cleanup dispatch reaches rule 2, emits stop:true. Asserts (a) success terminal outcome, (b) symlink at the recorded path still exists (rule-2 unlink was faulted, no further changes), (c) exactly one cleanup warning. Harness teardown removes the leftover symlink.
  - **T-TMP-42 (CLI) / T-TMP-42d (runPromise) / T-TMP-42e (run)**: `LOOPX_TEST_CLEANUP_FAULT=recursive-remove-fail`. Fixture observes LOOPX_TMPDIR, writes a few files inside (`file-1`, `sub/file-2`) so the dispatch reaches rule 4 on a non-empty identity-matched directory, emits stop:true. Asserts (a) success terminal outcome, (b) path observed still exists (rule-4 rmSync was faulted, no further changes), (c) exactly one cleanup warning. The 42d/42e suffix avoids collision with TEST-SPEC-reserved T-TMP-42a/42b/42c (cleanup-failure × terminal-error masking tests, still pending).
- Adjacent suites: zero regressions across programmatic-api (370), env-vars (96), wfdir (28). Full tmpdir.test.ts run: 453/453 in ~67s.

### ADR-0004 §7.4 (FIFO/socket/hard-link cleanup-rule-3 generic-dispatch coverage) — RESOLVED

SPEC §7.4 cleanup-safety dispatch case 3: "Path is a regular file, FIFO, socket, or other non-directory non-symlink: leave in place with a stderr warning. Unlinking would risk mutating unrelated data (hard-link `nlink` decrement, or data renamed into the path with `nlink == 1`)." The implementation in `cleanupTmpdir` (`packages/loop-extender/src/tmpdir.ts` lines 230-237) already conforms — after the rule-2 symlink early-return at lines 209-228, the dispatch is a generic `!stat.isDirectory()` negation: any non-directory non-symlink (regular file, FIFO, socket, or hard-link entry) hits the `emitCleanupWarning` + early-return branch. T-TMP-35 / 35a / 35b cover the regular-file branch on all three surfaces; T-TMP-37a / 37b / 37c extend coverage to FIFO, Unix-domain socket, and hard-link replacements on the CLI surface, proving the rule is typed generically rather than narrowly special-cased to regular files. T-TMP-37c additionally pins the SPEC §7.4 hard-link `nlink`-decrement-safety sub-clause that T-TMP-35's single-`nlink` regular-file fixture cannot exercise — a buggy implementation that classified the hard-link tmpdir entry as "ordinary regular file, safe to unlink because it has content we don't care about" would unlink it, decrementing `nlink` from 2 to 1 on the external path; T-TMP-35 would still pass on such an implementation but T-TMP-37c would catch the regression.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 435/435 PASS** — added 6 tests (3 IDs × 2 runtimes):
  - **T-TMP-37a**: CLI FIFO replacement. Bash fixture observes LOOPX_TMPDIR, `rm -rf "$LOOPX_TMPDIR"`, `mkfifo "$LOOPX_TMPDIR"` (path is now a named pipe), emits stop:true. Asserts: (a) exit 0, (b) `lstatSync(path).isFIFO()` is true (`isDirectory()` and `isSymbolicLink()` are false), (c) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line on stderr. Sets `NODE_ENV=test` to enable the structured marker line. Cleans up the leftover FIFO post-test.
  - **T-TMP-37b**: CLI Unix-domain socket replacement. `.ts` fixture (creating a SOCK_STREAM Unix-domain socket portably from bash is awkward) — observes LOOPX_TMPDIR, `rmSync` the directory, `net.createServer().listen(tmpdirPath)` to bind a socket inode at the path, then `output({ stop: true })`. UNIX-domain socket files are NOT auto-unlinked by the kernel on `process.exit()`, so the socket inode persists at cleanup time. Asserts: (a) exit 0, (b) `lstatSync(path).isSocket()` is true (and not a directory or symlink), (c) exactly one cleanup warning. Cleans up the leftover socket entry post-test.
  - **T-TMP-37c**: CLI hard-link replacement with `nlink`-decrement-safety assertion. Bash fixture creates an external regular file (`external-content`), `rm -rf "$LOOPX_TMPDIR"`, `ln "$external" "$LOOPX_TMPDIR"` (tmpdir path is now a hard link to the external file, both sharing one inode with `nlink == 2`). The script captures the pre-cleanup `nlink` count of the external path into a marker via portable stat invocation (`stat -c '%h'` GNU / `stat -f '%l'` BSD fallback). Asserts: (a) exit 0, (b) tmpdir-side path still exists as a regular file with content `external-content` (rule-3 leave-in-place), (c) external path also still exists with original content, (d) pre-cleanup `nlink` == 2 == post-cleanup external `nlink` (loopx did NOT decrement nlink by unlinking the tmpdir-side entry — the SPEC-cited rationale for rule 3), (e) tmpdir-side `nlink` also unchanged at 2, (f) `tmpdirLst.ino === externalLst.ino` (hard-link relationship intact), (g) exactly one cleanup warning. Includes an upfront same-device check (`lstatSync(project.dir).dev === lstatSync(tmpdirParent).dev`) — skips with a clear message if cross-device prevents unprivileged hard-link.
- Adjacent suites: zero regressions across programmatic-api (358), env-vars + wfdir (494/494). Full tmpdir.test.ts run: 435/435 in ~64s.

### ADR-0004 §7.4 (nested-symlink-recursive-walk cleanup-rule-4 surface-parity) — RESOLVED

SPEC §7.4 cleanup-safety dispatch case 4: "Path is a directory whose identity matches the recorded identity: recursively remove. Symlink entries encountered during the walk are unlinked but not traversed, so symlinks pointing outside the tmpdir do not collateral-delete their targets." The implementation in `cleanupTmpdir` (`packages/loop-extender/src/tmpdir.ts` lines 249-266) already conforms — when the top-level directory's `dev`/`ino` match the recorded identity, it calls `rmSync(path, { recursive: true, force: true })`. Node.js (and Bun's compat layer) performs lstat-based traversal: nested symlinks are unlinked as entries but their targets are never followed. Verified empirically: a nested `symlink -> external-target-dir` is removed during the recursive walk while the external target plus any contents survive intact. Successful rule-4 cleanup emits NO warning (warnings only fire on cleanup-failure or rules 3 / 5). T-TMP-37 / 37d / 37e pin this contract across all three execution surfaces — a buggy implementation that wired surface-specific recursive-walk dispatchers (e.g., a CLI-only `rmSync({recursive:true})` that conforms to rule 4 and a programmatic-driver path that traversed nested symlinks via a permissive recursive-delete primitive — silently deleting external target contents) would pass T-TMP-37 yet fail T-TMP-37d / 37e.

The fixture creates an external `external-target-dir/external-file` under PROJECT_ROOT, then a nested symlink at `$LOOPX_TMPDIR/nested-link -> $LOOPX_PROJECT_ROOT/external-target-dir`. The top-level entry is the identity-matched directory loopx created (rule 4 applies); only an entry INSIDE the tmpdir is a symlink, so the recursive walk's symlink-handling clause is what's under test. NODE_ENV=test is set so the structured marker is gated on; absence of the marker is then a load-bearing assertion (any erroneous warning would emit `LOOPX_TEST_CLEANUP_WARNING\t…`), not a trivially-true non-emission.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 429/429 PASS** — added 6 tests (3 IDs × 2 runtimes):
  - **T-TMP-37**: CLI nested-symlink recursive walk. Fixture observes LOOPX_TMPDIR, creates external `external-target-dir/external-file` with `external-content`, creates nested symlink `$LOOPX_TMPDIR/nested-link → external-target-dir`, emits stop:true. Asserts: (a) exit 0, (b) tmpdir path no longer exists (rule-4 recursive remove), (c) external dir + file with content `external-content` survive (no symlink-traversal collateral damage), (d) zero `LOOPX_TEST_CLEANUP_WARNING\t…` lines on stderr. Sets `NODE_ENV=test` to enable the structured marker line for load-bearing negative assertion.
  - **T-TMP-37d**: `runPromise()` counterpart. Same fixture, driven via `runAPIDriver` driver code that captures rejection state, observed-path existence, external dir + file existence + content into a JSON envelope. Asserts: (a) promise resolves with one Output, (b) tmpdir removed, (c) external dir + file + content survive, (d) zero cleanup warnings.
  - **T-TMP-37e**: `run()` generator counterpart. Same fixture, drained via `for await` in driver code. Asserts: (a) generator settles cleanly with one yield, (b) tmpdir removed, (c) external dir + file + content survive, (d) zero cleanup warnings.
- Adjacent suites: zero regressions across programmatic-api (370), env-vars (96), wfdir (28) → 494/494 in ~57s. Full tmpdir.test.ts run: 429/429 in ~65s.

### ADR-0004 §7.4 (mismatched-directory cleanup-rule-5 surface-parity) — RESOLVED

SPEC §7.4 cleanup-safety dispatch case 5: "Path is a directory whose identity does not match: leave in place with a stderr warning. loopx does not recursively remove a directory it did not create." The implementation in `cleanupTmpdir` (`packages/loop-extender/src/tmpdir.ts` lines 240-247) already conforms — when `lstat` returns a directory whose `dev`/`ino` (read with `bigint:true`) do not match the recorded identity from creation time, `emitCleanupWarning` fires with payload `cleanup of <path> skipped: identity mismatch` and the function returns without `rmSync`. Per-run cleanup-warning cardinality (SPEC §7.2) is exactly one because `CleanupState.warned` deduplicates. The warning does not affect the surfaced terminal outcome (CLI exit 0 / promise resolves / generator settles cleanly) per SPEC §7.4. T-TMP-36 / 36a / 36b pin this contract across all three execution surfaces — a buggy implementation that wired surface-specific dispatchers (e.g., a CLI-only mismatch-leave-in-place path and a programmatic-driver path that incorrectly `rmSync({ recursive: true, force: true })`'d the mismatched directory — silently destroying any same-user replacement contents that happened to be at the path) would pass T-TMP-36 yet fail T-TMP-36a / 36b.

The fixture uses a rename-aside pattern (`mv "$LOOPX_TMPDIR" "$LOOPX_TMPDIR-original-aside" && mkdir "$LOOPX_TMPDIR"`) rather than a naive `rm -rf … && mkdir …` to guarantee inode-distinctness. On POSIX filesystems where inode allocation does not strictly avoid recently-freed values, a rm/mkdir sequence could let the kernel coincidentally reuse the original inode for the freshly-created directory, making the new directory's identity fingerprint match loopx's captured one — the test would observe rule-4 success rather than rule-5 mismatch, an unrelated kernel-implementation accident. Keeping the original alive at a different path occupies its inode for the lifetime of the script, guaranteeing the freshly-created directory at $LOOPX_TMPDIR is allocated a distinct inode. As an incidental defense-in-depth side-effect, this fixture also asserts the renamed-aside copy survives loopx (already covered by T-TMP-33 "renamed-away tmpdirs are not chased").

Now-passing tests (this iteration):
- **tmpdir.test.ts: 423/423 PASS** — added 6 tests (3 IDs × 2 runtimes):
  - **T-TMP-36**: CLI mismatched-directory replacement. Fixture observes LOOPX_TMPDIR, renames it aside, mkdir + touch `mismatched-marker` at the original path, emits stop:true. Asserts: (a) exit 0, (b) tmpdir path still exists as a directory (`statSync().isDirectory()`), (c) `mismatched-marker` file inside still exists (rule-5 leave-in-place — loopx did not recursively remove the directory's contents), (d) renamed-aside copy survives, (e) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line on stderr. Sets `NODE_ENV=test` to enable the structured marker line. Cleans up both leftover paths post-test.
  - **T-TMP-36a**: `runPromise()` counterpart. Same fixture, driven via `runAPIDriver` driver code that captures rejection state, observed-path existence/dir-status, marker-file existence, and renamed-aside existence into a JSON envelope. Asserts: (a) promise resolves with one Output with stop:true, (b) tmpdir path still exists as a directory, (c) `mismatched-marker` intact, (d) renamed-aside survives, (e) exactly one cleanup warning.
  - **T-TMP-36b**: `run()` generator counterpart. Same fixture, drained via `for await`. Asserts: (a) generator settles cleanly with one yield (lastStop:true), (b) tmpdir path still exists as a directory, (c) `mismatched-marker` intact, (d) renamed-aside survives, (e) exactly one cleanup warning.
- Adjacent suites: zero regressions across programmatic-api (370), env-vars (96), wfdir (28). Full tmpdir.test.ts run: 423/423 in ~64s.

### ADR-0004 §7.4 (regular-file-replacement cleanup-rule-3 surface-parity) — RESOLVED

SPEC §7.4 cleanup-safety dispatch case 3: "Path is a regular file, FIFO, socket, or other non-directory non-symlink: leave in place with a stderr warning. Unlinking would risk mutating unrelated data (hard-link `nlink` decrement, or data renamed into the path with `nlink == 1`)." The implementation in `cleanupTmpdir` (`packages/loop-extender/src/tmpdir.ts` lines 230-236) already conforms — when `lstat` returns a non-symlink non-directory, `emitCleanupWarning` fires and the function returns without `unlinkSync`. Per-run cleanup-warning cardinality (SPEC §7.2) is exactly one because `CleanupState.warned` deduplicates. The warning does not affect the surfaced terminal outcome (CLI exit 0 / promise resolves / generator settles cleanly) per SPEC §7.4. T-TMP-35 / 35a / 35b pin this contract across all three execution surfaces — a buggy implementation that wired surface-specific dispatchers (e.g., a CLI-only `lstat`-and-warn path and a programmatic-driver path that incorrectly `unlinkSync`'d the regular file — silently mutating any `nlink`-shared inode) would pass T-TMP-35 yet fail T-TMP-35a / 35b.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 417/417 PASS** — added 6 tests (3 IDs × 2 runtimes):
  - **T-TMP-35**: CLI regular-file replacement. Fixture observes LOOPX_TMPDIR, `rm -rf` and replaces tmpdir with `printf '%s' "regular-file-replacement"` to make the path a regular file, emits stop:true. Asserts: (a) exit 0, (b) tmpdir path still exists as a regular file (`statSync().isFile()`) with content `regular-file-replacement` (rule-3 leave-in-place), (c) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line on stderr. Sets `NODE_ENV=test` to enable the structured marker line. Cleans up the leftover regular file at the recorded path post-test.
  - **T-TMP-35a**: `runPromise()` counterpart. Same fixture, driven via `runAPIDriver` driver code that captures rejection state, observed-path existence/file-status/content into a JSON envelope. Asserts: (a) promise resolves with one Output with stop:true, (b) tmpdir path still exists as a regular file with the expected content, (c) exactly one cleanup warning.
  - **T-TMP-35b**: `run()` generator counterpart. Same fixture, drained via `for await`. Asserts: (a) generator settles cleanly with one yield (lastStop:true), (b) tmpdir path still exists as a regular file with the expected content, (c) exactly one cleanup warning.
- Adjacent suites: zero regressions. Full tmpdir.test.ts run: 417/417 in ~62s.

### ADR-0004 §7.4 (symlink-replacement cleanup-rule-2 surface-parity) — RESOLVED

SPEC §7.4 cleanup-safety dispatch case 2: "Path is a symlink: unlink the symlink entry; do not follow the target." The implementation in `cleanupTmpdir` (`packages/loop-extender/src/tmpdir.ts` lines 208-228) already conforms — when `lstat` returns a symlink, it calls `unlinkSync(path)` and returns without traversing the target. Successful rule-2 cleanup emits no warning (warnings only emitted on cleanup-failure or rules 3 / 5). T-TMP-34 / 34a / 34b pin this contract across all three execution surfaces — a buggy implementation that wired surface-specific cleanup dispatchers (e.g., a CLI-only `unlinkSync`-on-symlink path and a programmatic-driver path that incorrectly traversed the symlink target via `rmSync({ recursive: true })` — silently deleting the external target) would pass T-TMP-34 yet fail T-TMP-34a / 34b.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 411/411 PASS** — added 6 tests (3 IDs × 2 runtimes):
  - **T-TMP-34**: CLI symlink-replacement cleanup. Fixture observes LOOPX_TMPDIR, creates external `target-survives/target-marker`, `rm -rf` and replaces tmpdir with symlink → external target, emits stop:true. Asserts: (a) exit 0, (b) tmpdir path no longer exists (rule-2 unlink), (c) external target survives with marker intact (no symlink-traversal collateral damage), (d) zero `LOOPX_TEST_CLEANUP_WARNING\t…` lines on stderr.
  - **T-TMP-34a**: `runPromise()` counterpart. Same fixture, driven via `runAPIDriver` driver code that captures rejection state, observed-path existence, and external target+marker existence into a JSON envelope. Asserts: (a) promise resolves with one Output, (b) tmpdir unlinked, (c) target+marker survive, (d) zero cleanup warnings.
  - **T-TMP-34b**: `run()` generator counterpart. Same fixture, drained via `for await` in driver code. Asserts: (a) generator settles cleanly with one yield, (b) tmpdir unlinked, (c) target+marker survive, (d) zero cleanup warnings.
- Adjacent suites: zero regressions. Full tmpdir.test.ts run: 411/411 in ~62s.

### ADR-0004 §7.4 (stale-tmpdir non-reaping + renamed-away ENOENT silence) — RESOLVED

SPEC §7.4 specifies that loopx does not reap stale `loopx-*` entries during CLI startup, CLI `loopx run` setup, or any per-run setup performed for `run()` / `runPromise()`. SPEC §7.4 also specifies that a script that renames its tmpdir defeats automatic cleanup (loopx does not chase renamed tmpdirs) and that the resulting ENOENT at cleanup time is a silent no-op (cleanup-safety rule 1).

The existing implementation already conformed:
- `createTmpdir` (`packages/loop-extender/src/tmpdir.ts`) only invokes `mkdtempSync(join(parent, "loopx-"))` — no parent-scan, no validation, no removal of pre-existing entries.
- `cleanupTmpdir` rule 1 (lines 196-199) returns silently on ENOENT without emitting any warning.
- `bin.ts` dispatches to help / version / unknown-command paths via `process.exit()` before any tmpdir-related code runs, so non-`run` CLI startup never creates or scans tmpdir.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 405/405 PASS** — added 16 tests (7 sub-tests × 2 runtimes for T-TMP-32 series + 1 × 2 for T-TMP-33):
  - **T-TMP-32**: CLI `loopx run` setup leaves pre-existing `loopx-stale-xyz/` intact + cleans up its own tmpdir after run (only stale entry remains under parent).
  - **T-TMP-32a**: Same contract on `runPromise()` (eager-snapshot path per SPEC §9.2).
  - **T-TMP-32b**: Same contract on `run()` (lazy-snapshot path per SPEC §9.1).
  - **T-TMP-32c**: 4 sub-cases over non-`run` CLI startup — `loopx -h` (exit 0), `loopx version` (exit 0), `loopx` (no args, exit 0), `loopx --unknown` (parser error, exit 1). All assert: stale entry survives, no new `loopx-*` materialized under parent, expected exit code.
  - **T-TMP-33**: Fixture renames `$LOOPX_TMPDIR` to `$LOOPX_TMPDIR-renamed` mid-run. Asserts: original path absent, renamed path present with marker intact, **zero** `LOOPX_TEST_CLEANUP_WARNING\t…` lines on stderr (ENOENT-at-cleanup is silent per SPEC §7.4 cleanup-rule-1, completing the warning-cardinality characterization across the cleanup-dispatch tree alongside T-TMP-35/T-TMP-36 which assert exactly one warning for non-ENOENT mismatched-identity / regular-file replacements).
- Adjacent suites: zero regressions. Full tmpdir.test.ts run: 405/405 in ~61s.

### ADR-0004 §7.4 (cleanup-fault-warning-does-not-mask script-error terminal surface-parity) — RESOLVED

SPEC §7.4 final paragraph: "warnings do not affect the CLI exit code, the generator outcome, or the promise rejection reason." T-TMP-40/41/42 + T-TMP-40a/40b/41a/41b/42d/42e pin this rule on the SUCCESS terminal across all three cleanup-fault seams (lstat-fail / symlink-unlink-fail / recursive-remove-fail) × all three execution surfaces. T-TMP-35c/35d/35e pin the rule on the SCRIPT-ERROR terminal × the two natural cleanup-safety dispatch branches (rule 3 regular-file replacement, rule 5 mismatched-directory replacement). T-TMP-42a closes the matrix: the SCRIPT-ERROR terminal × all three cleanup-fault seams × all three surfaces.

The implementation in `runLoop` (`packages/loop-extender/src/loop.ts:120-217`) already conformed: the iteration body wraps in `try/finally` so `cleanupTmpdir(tmpdirResource, cleanupState)` (line 216) runs before the script-failure `throw new Error('Script <wf>:<scr> exited with code <n>')` (line 133-135) propagates out of the generator. The fault seam fires inside `cleanupTmpdir` (top-of-`cleanupTmpdir` for `lstat-fail`, rule-2 unlinkSync for `symlink-unlink-fail`, rule-4 rmSync for `recursive-remove-fail`) and emits via `emitCleanupWarning` which writes to stderr but never rewrites the in-flight throw value.

The CLI tests carry an observably-disjoint-lines assertion: a distinctive `SCRIPT-FAILURE-MARKER-T-TMP-42A` line printed to stderr by the script before its tampering must reach the user's stderr on a separate line from the `LOOPX_TEST_CLEANUP_WARNING\t…` marker. Since both paths produce CLI exit 1, the disjoint-lines assertion is what catches a buggy implementation that wrapped or replaced the script's stderr with a cleanup-classifier wrapper. The API tests carry an error-message-shape probe: the rejection reason (runPromise) / throw value (run) must match the loop.ts contract (regex `/exited with code/`) and must NOT contain "cleanup" anywhere in the message.

Per-fault tampering required to make the seam fire on the failure path:
- **lstat-fail**: seam fires at the top-level lstat in cleanupTmpdir; no path manipulation needed.
- **symlink-unlink-fail**: seam fires only when dispatch reaches rule 2 (path is a symlink), so the script must `rm -rf` the tmpdir and replace it with `ln -s /tmp` before exit 1.
- **recursive-remove-fail**: seam fires only when dispatch reaches rule 4 (identity-matched directory); the original loopx-created directory satisfies that. Populating the directory matches the success-terminal T-TMP-42 fixture.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 483/483 PASS** — added 18 tests (3 fault seams × 3 surfaces × 2 runtimes):
  - **T-TMP-42a (lstat-fail / symlink-unlink-fail / recursive-remove-fail) [CLI]**: `runCLI(['run', '-n', '1', 'ralph'])` with `LOOPX_TEST_CLEANUP_FAULT=<seam>` and `NODE_ENV=test`. Asserts: (a) exit 1 (script failure preserved), (b) exactly one `SCRIPT-FAILURE-MARKER-T-TMP-42A` line on stderr, (c) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line, (d) marker and warning lines are observably disjoint, (e) path persistence per fault seam (lstat-fail: original directory exists; symlink-unlink-fail: symlink exists; recursive-remove-fail: directory exists).
  - **T-TMP-42a (lstat-fail / symlink-unlink-fail / recursive-remove-fail) [runPromise]**: driver code awaits `runPromise("ralph", { maxIterations: 1 })`, captures rejection state, error message, observed-path file/symlink type. Asserts: (a) promise rejected, (b) errMsg matches `/exited with code/` (script-failure shape) and does NOT contain "cleanup", (c) exactly one cleanup warning, (d) path persistence per fault seam.
  - **T-TMP-42a (lstat-fail / symlink-unlink-fail / recursive-remove-fail) [run]**: driver code drives `run("ralph", { maxIterations: 1 })` via `for await`, captures throw state and error message. Asserts: (a) generator threw, (b) errMsg matches `/exited with code/` and does NOT contain "cleanup", (c) exactly one cleanup warning, (d) path persistence per fault seam.
- Adjacent suites: zero regressions across programmatic-api + env-vars + wfdir → 494/494. Full tmpdir.test.ts run: 483/483 in ~70s.

### ADR-0004 §7.2 / §7.4 (LOOPX_TEST_TERMINAL_TRIGGER_PAUSE seam — cleanup-start window + first-trigger-wins) — RESOLVED

SPEC §7.2: "the first terminal trigger observed by loopx determines the surfaced outcome among genuinely racing triggers." TEST-SPEC §1.4 specifies a `LOOPX_TEST_TERMINAL_TRIGGER_PAUSE` seam (gated on `NODE_ENV=test`) whose value names a window inside the run lifecycle at which the implementation pauses for a bounded interval (≥2s ≤10s) AFTER writing a parent-observable JSON marker to the path named by `LOOPX_TEST_TERMINAL_TRIGGER_PAUSE_MARKER`, so a polling harness can deterministically know when to deliver a racing second terminal trigger. This iteration implements the seam at the `cleanup-start` window value and pins the SPEC §7.2 contract for the racing-SIGINT→SIGTERM case on the rule-3 and rule-5 cleanup-warning branches.

Implementation (`packages/loop-extender/src/tmpdir.ts`): the seam fires INSIDE `cleanupTmpdir` after the `state.attempted` idempotence check, so a second cleanup call on the same state is still a no-op (no second pause). Marker write: `openSync` / `writeSync({"window":"cleanup-start"})` / `fsyncSync` / `closeSync` BEFORE the bounded delay begins (per TEST-SPEC §1.4 ordering — marker visibility implies pause is about to start). Sync sleep: `Atomics.wait(view, 0, 0, 3000)` on a tiny SharedArrayBuffer — within the spec-required 2-10s range, no spawn dependency, no event-loop entanglement during the wait. Seam is gated on `NODE_ENV=test` AND the env-var equaling `cleanup-start`; all other values (or unset) are no-ops. Marker write is best-effort (if marker path is unwritable, the pause still happens).

SPEC §7.2 first-trigger-wins fix (`packages/loop-extender/src/bin.ts`): the previous CLI signal handler unconditionally executed `receivedSignal = sig; ac.abort(sig);`, which meant a SIGTERM observed during cleanup-of-prior-SIGINT would overwrite the first observation and the surfaced exit code would shift from 130 to 143. The fix: only set `receivedSignal` if currently `null`; the second `ac.abort()` still propagates (no-op since already aborted) but `receivedSignal` stays anchored at the first observation. This is a real implementation correctness fix for SPEC §7.2, not just a test-fixture concern — any production scenario with two close-together signals exercised the same overwrite bug.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 503/503 PASS** — added 4 tests (2 IDs × 2 runtimes):
  - **T-TMP-38**: CLI cleanup idempotence under racing SIGINT→SIGTERM on the SPEC §7.4 rule-3 (regular-file replacement) cleanup-warning branch. Driven via `runCLIWithSignal`; the harness polls for the `LOOPX_TEST_TERMINAL_TRIGGER_PAUSE_MARKER` path written by the seam (existence implies cleanup paused at the `cleanup-start` window), then delivers the racing SIGTERM. Asserts: (a) exit code 130 (SIGINT survives the SIGTERM-during-pause race per SPEC §7.2 first-trigger-wins), (b) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line on stderr (per-run cleanup-warning cardinality holds across racing triggers — `CleanupState.warned` deduplicates), (c) regular-file at the recorded tmpdir path with content `regular-file-replacement` survives (rule-3 leave-with-warning preserved). Carries `{ timeout: 60_000, retry: 2 }` per the racing-trigger flakiness convention.
  - **T-TMP-39**: same race configuration as T-TMP-38 but on the SPEC §7.4 rule-5 (mismatched-directory replacement via rename-aside pattern) cleanup-warning branch. Asserts: (a) exit code 130, (b) exactly one cleanup warning, (c) mismatched-directory replacement with `mismatched-marker` file inside survives, (d) renamed-aside copy at `<loopx-tmpdir-name>-aside` also survives.
- Adjacent suites: zero regressions across programmatic-api + env-vars + wfdir (494/494), signals (9/9), loop-state + execution + cli-basics (449/449), unit + harness (158/158).

### ADR-0004 §7.4 (cleanup-fault-warning-does-not-mask CLI signal terminal surface) — RESOLVED

SPEC §7.4 final paragraph: "warnings do not affect the CLI exit code, the generator outcome, or the promise rejection reason." The contract holds for both the cleanup-safety warning branch and the cleanup-failure warning branch. T-TMP-35f covers the cleanup-safety side of the signal terminal (rule 3 / rule 5 × SIGINT/SIGTERM × CLI). T-TMP-40 / T-TMP-41 / T-TMP-42 cover the cleanup-failure side on the success terminal across all three surfaces. T-TMP-42a covers cleanup-failure × script-error across all three surfaces. T-TMP-42b covers cleanup-failure × abort across the two programmatic surfaces. T-TMP-42c closes the remaining diagonal: the cleanup-failure warning branch × CLI signal terminal — the only diagonal not previously pinned in the cleanup-warning-does-not-affect-outcome matrix.

A buggy implementation that wired the signal-terminal `128 + signal-number` exit-code path correctly for rule-3 / rule-5 cleanup-safety warnings (passing T-TMP-35f) yet routed cleanup-failure warnings (lstat-fail / symlink-unlink-fail / recursive-remove-fail) through a separate stream that promoted the warning to a cleanup-failure-class exit code under signal — exit 1 or some implementation-specific cleanup-failure code instead of the signal code — would pass T-TMP-35f and T-TMP-40 / T-TMP-41 / T-TMP-42 / T-TMP-42a / T-TMP-42b yet fail this test.

Implementation in `runLoop` (`packages/loop-extender/src/loop.ts`) and the CLI signal forwarder (`packages/loop-extender/src/bin.ts`) already conformed: the iteration body wraps in `try/finally` so `cleanupTmpdir(...)` runs before any signal-driven abort error propagates out of the generator, and the CLI's signal handler observes the first signal as the surfaced terminal trigger. The cleanup-fault seam (`LOOPX_TEST_CLEANUP_FAULT={lstat-fail, symlink-unlink-fail, recursive-remove-fail}`) fires inside `cleanupTmpdir` and emits via `emitCleanupWarning` which writes to stderr but never rewrites the in-flight throw value or shifts the CLI exit code.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 515/515 PASS** — added 12 tests (3 cleanup-fault seams × 2 signals × 2 runtimes):
  - **T-TMP-42c (lstat-fail × SIGINT, lstat-fail × SIGTERM, symlink-unlink-fail × SIGINT, symlink-unlink-fail × SIGTERM, recursive-remove-fail × SIGINT, recursive-remove-fail × SIGTERM)**: `runCLIWithSignal(['run', '-n', '1', 'ralph'])` with `LOOPX_TEST_CLEANUP_FAULT=<seam>` and `NODE_ENV=test`. Fixture observes LOOPX_TMPDIR into an external marker (path external to the tmpdir so it survives cleanup), performs the seam-specific path manipulation when needed (for `symlink-unlink-fail`: `rm -rf "$LOOPX_TMPDIR" && ln -s /tmp "$LOOPX_TMPDIR"` so cleanup dispatch reaches rule 2; for `recursive-remove-fail`: writes a few files inside the tmpdir so the dispatch reaches rule 4 on a non-empty identity-matched directory; for `lstat-fail`: no path manipulation needed, the seam fires on the top-level `lstat`), writes `"ready"` to stderr, then blocks indefinitely. Harness waits for `"ready"` then sends the variant-specific signal. Asserts: (a) loopx exits with `130` (SIGINT) or `143` (SIGTERM) — the cleanup-failure warning did not mask, replace, or override the signal exit code (load-bearing), (b) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line on stderr (per-run cleanup-warning cardinality holds across the signal terminal), (c) path persistence per fault seam (lstat-fail: identity-matched directory remains; symlink-unlink-fail: symlink remains; recursive-remove-fail: directory remains).
- Adjacent suites: zero regressions across programmatic-api + env-vars + wfdir + signals + loop-state + execution + cli-basics (943/943), unit + harness (158/158). Full tmpdir.test.ts run: 515/515 in ~85s.

### ADR-0004 §7.2 / §7.4 (T-TMP-38f cleanup idempotence under racing SIGTERM → SIGINT — inverted-order parity) — RESOLVED

SPEC §7.2's first-observed-trigger-wins rule and SPEC §7.4's at-most-one-cleanup-attempt / at-most-one-cleanup-warning rules apply symmetrically across SIGINT and SIGTERM — neither signal is privileged. T-TMP-38 covered the SIGINT-first × SIGTERM-second-during-cleanup race on the rule-3 regular-file warning branch; T-TMP-39 covered the same SIGINT-first race on the rule-5 mismatched-directory branch; T-TERM-03 (still pending) covers the same SIGINT-first race on the surfaced-exit-code axis. None of those exercises the inverted observation order. T-TMP-38f closes that gap: a buggy implementation that special-cased SIGINT-first cleanup-race handling (e.g., a SIGINT-handler path that correctly gated cleanup-idempotence and exit-code precedence, but a SIGTERM-handler path that re-entered cleanup or surfaced the second signal's exit code) would pass T-TMP-38 / T-TMP-39 / T-TERM-03 yet fail T-TMP-38f.

The implementation in `bin.ts` (`signalHandler` at lines 652-657) already conformed: `receivedSignal` is set only when currently `null`, so the first observed signal pins the surfaced exit code regardless of which signal arrives first. The `cleanup-start` seam in `tmpdir.ts` (`maybePauseAtCleanupStart` at lines 105-137) is signal-agnostic — it pauses on cleanup entry and writes the parent-observable marker without inspecting which signal initiated the cleanup. `CleanupState.attempted` deduplicates the cleanup attempt so the racing post-pause SIGINT does not re-enter `cleanupTmpdir`.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 517/517 PASS** — added 2 tests (1 ID × 2 runtimes):
  - **T-TMP-38f**: Same fixture and seam configuration as T-TMP-38 (rule-3 regular-file replacement, `LOOPX_TEST_TERMINAL_TRIGGER_PAUSE=cleanup-start`). Driven via `runCLIWithSignal`; the harness waits for the script's `ready` marker on stderr, sends **SIGTERM** (the first-observed signal in the inverted order), polls for the `LOOPX_TEST_TERMINAL_TRIGGER_PAUSE_MARKER` path written by the seam (existence implies cleanup paused at the `cleanup-start` window), then delivers the racing **SIGINT** during the bounded pause. Asserts (a) exit code 143 (SIGTERM survives the SIGINT-during-cleanup race per SPEC §7.2 first-trigger-wins applied symmetrically across signals — load-bearing: a buggy SIGTERM-handler that re-pinned `receivedSignal` to the second SIGINT would shift the exit code to 130), (b) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line on stderr (per-run cleanup-warning cardinality holds across racing triggers regardless of signal-order), (c) regular-file at the recorded tmpdir path with content `regular-file-replacement` survives (rule-3 leave-with-warning preserved). Carries `{ timeout: 60_000, retry: 2 }` per the racing-trigger flakiness convention.
- Adjacent suites: zero regressions across signals + programmatic-api + env-vars + wfdir.

### ADR-0004 §7.4 (cleanup-warning-does-not-mask signal/abort terminal surface-parity) — RESOLVED

SPEC §7.4: "cleanup warnings do not affect the CLI exit code, the generator outcome, or the promise rejection reason." T-TMP-35 / 35a / 35b (rule-3 success terminal) and T-TMP-36 / 36a / 36b (rule-5 success terminal) pinned the contract on `stop: true` script termination. T-TMP-35c / 35d / 35e extended the contract to the SCRIPT-ERROR terminal × both warning-emitting cleanup-safety branches (rule 3 regular-file replacement, rule 5 mismatched-directory replacement). T-TMP-35f / 35g / 35h close the matrix on the remaining terminals: SIGNAL (CLI SIGINT/SIGTERM) and ABORT (programmatic API), again × both warning-emitting cleanup-safety branches × all three execution surfaces.

The implementation in `runLoop` (`packages/loop-extender/src/loop.ts:120-217`) already conformed: the iteration body wraps in `try/finally` so `cleanupTmpdir(tmpdirResource, cleanupState)` (line 216) runs before any abort error propagates out of the generator. The user signal → internal AbortController propagation (`packages/loop-extender/src/run.ts:279-303` per SPEC §9.5) wires the abort terminal correctly so a CLI SIGINT/SIGTERM or programmatic abort reaches the in-flight iteration. The active child process group is terminated per SPEC §9.1; T-TMP-35g and T-TMP-35h verify this explicitly via pid-marker probing (assert the script's pid is no longer alive after the rejection / throw is observed).

Fixture: a shared `buildSignalAbortCleanupFixture` helper in tmpdir.test.ts. The fixture tampers with `$LOOPX_TMPDIR` per cleanup-safety branch (rule 3: `rm -rf "$LOOPX_TMPDIR"` followed by writing a regular file at the path; rule 5: `mv "$LOOPX_TMPDIR" "$LOOPX_TMPDIR-original-aside" && mkdir "$LOOPX_TMPDIR" && touch "$LOOPX_TMPDIR/mismatched-marker"`), writes the original tmpdir path to a `tmpdirObservation` marker file AFTER tampering completes (existence-of-marker implies tampering complete — important for API drivers that poll for the marker before triggering abort), writes the script's pid to a `pidMarker` for child-termination probing, prints `ready` to stderr (consumed by CLI signal coordinator via `runCLIWithSignal`'s `waitForStderr`), then blocks forever (so the run cannot terminate by any means other than the signal/abort under test).

Now-passing tests (this iteration):
- **tmpdir.test.ts: 499/499 PASS** — added 16 tests:
  - **T-TMP-35f (regular-file × SIGINT, regular-file × SIGTERM, mismatched-dir × SIGINT, mismatched-dir × SIGTERM) [CLI]**: 4 sub-cases × 2 runtimes = 8 tests. Driven via `runCLIWithSignal` (waits for the script's `ready` marker on stderr, then sends SIGINT or SIGTERM). Asserts: (a) exit code 130 (SIGINT) or 143 (SIGTERM) — cleanup warning does NOT promote the signal terminal into a cleanup-failure exit code, (b) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line on stderr, (c) path persistence per cleanup-safety branch (rule 3: regular file at the recorded path with the test-written content; rule 5: directory at recorded path with `mismatched-marker` file inside, plus the renamed-aside copy).
  - **T-TMP-35g (regular-file, mismatched-dir) [runPromise]**: 2 sub-cases × 2 runtimes = 4 tests. Driver code awaits `runPromise("ralph", { signal })`, polls for the `tmpdirObservation` marker (existence implies fixture tampering complete), calls `ac.abort()` to trigger the abort terminal, captures rejection state and error message into a JSON envelope. Asserts: (a) promise rejected with abort error (`errName === "AbortError"` or `/abort/i.test(errMsg)`) and message does NOT contain "cleanup" — the abort error is not displaced by a cleanup-failure error, (b) exactly one cleanup warning, (c) child PG terminated (the script's pid recorded in `pidMarker` is no longer alive — verified per SPEC §9.1 via `process.kill(pid, 0)` returning ESRCH), (d) path persistence per branch.
  - **T-TMP-35h (regular-file, mismatched-dir) [run]**: 2 sub-cases × 2 runtimes = 4 tests. Driver code drives `run("ralph", { signal })` via `for await`, polls for the `tmpdirObservation` marker, calls `ac.abort()`, captures throw state and error message. Asserts: (a) generator threw abort error, (b) exactly one cleanup warning, (c) child PG terminated, (d) path persistence per branch.
- Adjacent suites: zero regressions across programmatic-api (370), env-vars (96), wfdir (28) → 494/494. Full tmpdir.test.ts run: 499/499 in ~73s.

### ADR-0004 §7.4 (cleanup-warning-does-not-mask script-error terminal surface-parity) — RESOLVED

SPEC §7.4: "cleanup warnings do not affect the CLI exit code, the generator outcome, or the promise rejection reason." T-TMP-35 / 35a / 35b (rule-3 success terminal) and T-TMP-36 / 36a / 36b (rule-5 success terminal) pin this against `stop: true` script termination. T-TMP-35c / 35d / 35e pin the contract on the script-error terminal — when a script triggers a cleanup-safety rule (rule 3 regular-file replacement or rule 5 mismatched-directory replacement) AND exits non-zero, the surfaced terminal must remain the SCRIPT failure (CLI exit 1 with the script's stderr passed through; runPromise rejects with the script-failure error; run() throws the script-failure error), not a cleanup-failure-class outcome. The implementation in `runLoop` (`packages/loop-extender/src/loop.ts:120-217`) already conformed: the iteration body wraps in `try/finally` so `cleanupTmpdir(tmpdirResource, cleanupState)` (line 216) runs before the script-failure `throw new Error('Script <wf>:<scr> exited with code <n>')` (line 133-135) propagates out of the generator. Cleanup `emitCleanupWarning` writes to stderr but never rewrites the in-flight throw value.

The CLI test (T-TMP-35c) carries an observably-disjoint-lines assertion: a distinctive `SCRIPT-FAILURE-MARKER-T-TMP-35C` line printed to stderr by the script before its tampering must reach the user's stderr on a separate line from the `LOOPX_TEST_CLEANUP_WARNING\t…` marker. Since both the script-failure path and a hypothetical cleanup-promoted-to-failure path produce CLI exit 1, exit-code alone cannot distinguish — the disjoint-lines assertion is what catches a buggy implementation that wrapped or replaced the script's stderr with a cleanup-classifier wrapper.

The API tests (T-TMP-35d / 35e) carry an error-message-shape probe: the rejection reason (runPromise) / throw value (run) must match the loop.ts contract `Script '<wf>:<scr>' exited with code <n>` (regex `/exited with code/`) and must NOT contain "cleanup" anywhere in the message. A buggy implementation that wrapped the script-failure error in a cleanup-failure error would fail this assertion.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 465/465 PASS** — added 12 tests (3 IDs × 2 cleanup-safety variants × 2 runtimes):
  - **T-TMP-35c (regular-file-replacement, mismatched-directory-replacement) [CLI]**: `runCLI(['run', '-n', '1', 'ralph'])`. Asserts: (a) exit 1 (from script), (b) exactly one `SCRIPT-FAILURE-MARKER-T-TMP-35C` line on stderr, (c) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line, (d) marker line and warning line are disjoint, (e) path persistence per cleanup-safety branch (rule 3: regular file with content `regular-file-replacement` survives; rule 5: directory with `mismatched-marker` and the renamed-aside copy survive).
  - **T-TMP-35d (regular-file-replacement, mismatched-directory-replacement) [runPromise]**: driver code awaits `runPromise("ralph", { maxIterations: 1 })`, captures rejection state, error message, and post-run path observations. Asserts: (a) promise rejected, (b) errMsg matches `/exited with code/` (script-failure shape) and does NOT contain "cleanup", (c) exactly one cleanup warning, (d) path persistence per branch.
  - **T-TMP-35e (regular-file-replacement, mismatched-directory-replacement) [run]**: driver code drives `run("ralph", { maxIterations: 1 })` via `for await`, captures throw state and error message. Asserts: (a) generator threw, (b) errMsg matches `/exited with code/` and does NOT contain "cleanup", (c) exactly one cleanup warning, (d) path persistence per branch.
- Adjacent suites: zero regressions across programmatic-api (370), env-vars (96), wfdir (28) → 494/494. Full tmpdir.test.ts run: 465/465 in ~70s.

### ADR-0004 §7.2 / §7.4 (T-TMP-38a — first-observed-wins for racing abort → .return() during cleanup-start, run() surface) — RESOLVED

The SPEC §7.2 contract: "the first terminal trigger observed by loopx determines the surfaced outcome among genuinely racing triggers" applies symmetrically across all terminal-trigger pairs. T-TMP-38 / T-TMP-38f / T-TMP-39 pinned the rule for signal-vs-signal races on the CLI surface. T-TMP-38a closes one of the diagonals on the run() programmatic surface: abort observed first, .return() racing during the cleanup-start pause.

Implementation gaps that this iteration closed:
- **Async seam pause (`packages/loop-extender/src/tmpdir.ts`):** the `cleanup-start` seam previously used `Atomics.wait` on a SharedArrayBuffer (sync sleep blocking the JS thread for 3 sec). For CLI tests, sync vs async pause was indistinguishable — the test process and loopx CLI subprocess are separate processes. For a same-process driver (programmatic API tests), the sync sleep blocked the driver's event loop, preventing it from polling for the parent-observable cleanup-start marker and calling `gen.return()` / `ac.abort()` mid-pause. Replaced `syncSleep` with `asyncSleep` (`setTimeout`-based Promise). Made `maybePauseAtCleanupStart` async, `cleanupTmpdir` async, and `createTmpdir` async (since its error path may invoke `cleanupTmpdir`). `runLoop`'s finally now `await`s `cleanupTmpdir(...)`. T-TMP-38 / T-TMP-39 / T-TMP-38f all still pass with the async pause (signal handler runs during the async wait instead of after the sync wait — observably equivalent for cross-process coordination).
- **Wrapper first-observed-wins (`packages/loop-extender/src/run.ts`):** the wrapper's `.return()` / `.throw()` / `[Symbol.asyncDispose]` previously pinned `returnCalled = true` unconditionally. The wrapper's `.next()` catch then silenced the abort error from a pending in-flight iteration when `returnCalled` was true (the SPEC §9.1 silent-clean-completion contract). For an abort-first-then-.return() race, this incorrectly inverted the surfaced outcome — silent completion instead of abort error. Fixed: each consumer-cancellation method now reads `internalAc.signal.aborted` at entry; only pins `returnCalled` if abort was NOT first-observed. The inner generator's `.return()` is still driven so the runLoop `finally` (`cleanupTmpdir`) runs and the inner gen settles. SPEC §9.3 post-final-yield-abort carve-out preserved (existing path unchanged for that case).

Now-passing tests (this iteration):
- **tmpdir.test.ts: 519/519 PASS** — added 2 tests (1 ID × 2 runtimes):
  - **T-TMP-38a**: bash fixture observes nothing (rule-3 replacement is sufficient — fixture `rm -rf "$LOOPX_TMPDIR" && printf '%s' "regular-file-replacement" > "$LOOPX_TMPDIR" && touch "$FIXTURE_READY_PATH" && while true; do sleep 1; done`). Driver code (run via `runAPIDriver`) creates an `AbortController`, kicks off `for await (const _ of run("ralph", { signal: ac.signal }))` in an async IIFE that captures any thrown error into `errName` / `errMessage`, polls for the fixture-ready file, calls `ac.abort()`, polls for the parent-observable cleanup-start marker (existence implies cleanup paused at the seam), then calls `await gen.return()` (during the seam pause), then awaits the IIFE. Writes a JSON envelope `{ errName, errMessage }` to stdout. Asserts: (a) `envelope.errName === "AbortError"` (load-bearing — a buggy wrapper that pinned `returnCalled` unconditionally would silence the abort error and produce `errName === ""`), (b) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line on stderr, (c) regular-file at the recorded tmpdir path with content `regular-file-replacement` survives.
- Adjacent suites: zero regressions across programmatic-api (370), signals (9), env-vars (96), wfdir (28), loop-state (96), execution (45), unit (143), harness (15).

### ADR-0004 §7.2 / §7.4 (T-TMP-38b / T-TMP-38b-run — first-observed-wins for racing non-zero-script-exit → abort during cleanup-start, runPromise() and run() surfaces) — RESOLVED

The SPEC §7.2 contract: "the first terminal trigger observed by loopx determines the surfaced outcome among genuinely racing triggers" applies symmetrically across all terminal-trigger pairs. T-TMP-38a / T-TMP-38a2 covered the abort-vs-consumer-cancellation diagonal on the run() surface. T-TMP-38b / T-TMP-38b-run cover the non-zero-script-exit-vs-abort diagonal on the runPromise() and run() surfaces. Together the family pins SPEC §7.2 first-observed-wins symmetrically across multiple trigger pairs at the cleanup-start window.

The implementation in `runLoop` (`packages/loop-extender/src/loop.ts`) and the `run.ts` wrapper ALREADY conformed: when the script exits non-zero, runLoop throws `Error('Script <wf>:<scr> exited with code <n>')` from the iteration body. The throw enters the try/finally which awaits `cleanupTmpdir`. cleanupTmpdir pauses at the `cleanup-start` seam (`maybePauseAtCleanupStart` in tmpdir.ts) for ~3s after writing the parent-observable marker. During the pause, the driver code (running in parallel via the iterPromise IIFE for run(), or directly under runPromise's for-await loop) calls `ac.abort()`. This sets `internalAc.signal.aborted = true` but the in-flight throw is unaffected. cleanup completes (rule-3 emits exactly one warning), the throw propagates out, and wrapper.next() catches with `returnCalled === false` → throws err. The script-failure error reaches the consumer (runPromise rejects; for-await re-throws to the iterPromise's catch).

Now-passing tests (this iteration):
- **tmpdir.test.ts: 525/525 PASS** — added 4 tests (2 IDs × 2 runtimes):
  - **T-TMP-38b**: bash fixture (rule-3 regular-file replacement, identical structure to T-TMP-38a's fixture but exits 1 instead of blocking — `rm -rf "$LOOPX_TMPDIR" && printf '%s' "regular-file-replacement" > "$LOOPX_TMPDIR" && touch "$FIXTURE_READY_PATH" && exit 1`). Driver code (run via `runAPIDriver`) creates an `AbortController`, kicks off `runPromise("ralph", { signal: ac.signal, maxIterations: 1 })`, polls for the fixture-ready file, polls for the parent-observable cleanup-start marker (existence implies cleanup paused at the seam), then calls `ac.abort()` during the bounded pause. Awaits the promise inside try/catch capturing `errName` / `errMessage`. Writes a JSON envelope `{ errName, errMessage }` to stdout. Asserts: (a) `envelope.errMessage` matches `/exited with code/` AND `envelope.errName !== "AbortError"` AND `envelope.errMessage` does NOT match `/abort/i` (script-failure preserved — first-observed-wins; load-bearing — a buggy implementation that wrapped the script-failure error in an abort-class wrapper would fail), (b) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line on stderr (no second cleanup attempt), (c) regular-file at the recorded tmpdir path with content `regular-file-replacement` survives.
  - **T-TMP-38b-run**: bash fixture (same as T-TMP-38b). Driver code creates an `AbortController`, drives `for await (const _ of run("ralph", { signal: ac.signal, maxIterations: 1 }))` in an async IIFE that captures any thrown error into `errName` / `errMessage`, polls for fixture-ready, polls for cleanup-start marker, calls `ac.abort()` during the pause, then awaits the IIFE. Writes the same JSON envelope. Asserts: same shape as T-TMP-38b — verifies that a buggy implementation that wired idempotence correctly on `runPromise` rejection but routed `run()` settlement through a different cleanup-dispatcher would fail this counterpart.
- Adjacent suites: zero regressions across programmatic-api + env-vars + wfdir + loop-state (590), signals (9). Full tmpdir.test.ts run: 525/525 in ~118s.

### ADR-0004 §7.2 / §7.4 (T-TMP-38a2 — first-observed-wins for racing .return() → abort during cleanup-start, run() surface, inverted-order parity) — RESOLVED

Inverted-order parity counterpart to T-TMP-38a: where T-TMP-38a covered abort-first × .return()-second on the run() programmatic surface, T-TMP-38a2 covers .return()-first × abort-second on the same run() surface. Together the pair pins SPEC §7.2 first-observed-wins symmetrically across the consumer-cancellation-vs-abort diagonal at the cleanup-start window.

The implementation in `packages/loop-extender/src/run.ts` ALREADY conformed: when `.return()` is the first observed terminal trigger (`internalAc.signal.aborted === false` at .return() entry), the wrapper sets `returnCalled = true`. The post-pause abort (during the cleanup-start seam) then propagates through the runLoop body, but the wrapper.next() catch silences the abort error per the consumer-cancellation contract. CleanupState.attempted gates the single cleanup attempt across both observation orders. No implementation change was required this iteration — only the test was authored.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 521/521 PASS** — added 2 tests (1 ID × 2 runtimes):
  - **T-TMP-38a2**: bash fixture (rule-3 regular-file replacement, identical to T-TMP-38a's fixture). Driver code (run via `runAPIDriver`) creates an `AbortController`, drives `for await (const _ of run("ralph", { signal: ac.signal }))` in an async IIFE that captures any thrown error into `errName` / `errMessage`, polls for the fixture-ready file, calls `gen.return(undefined)` first (first observed terminal trigger), polls for the parent-observable cleanup-start marker (existence implies cleanup paused at the seam), then calls `ac.abort()` during the bounded pause, then awaits the `returnPromise` and the `iterPromise` (IIFE). Writes a JSON envelope `{ returnDone, returnValue, errName, errMessage }` to stdout. Asserts: (a) `envelope.returnDone === true` AND `envelope.returnValue === null` AND `envelope.errName === ""` (clean settlement, no abort surfaced — first-observed-wins applied symmetrically: .return() was first, so the abort error is silenced by the consumer-cancellation contract), (b) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line on stderr (no second cleanup attempt), (c) regular-file at the recorded tmpdir path with content `regular-file-replacement` survives.
- Adjacent suites: zero regressions across programmatic-api (370), env-vars (96), wfdir (28) → 494/494. signals (9). loop-state (96), execution (45). tmpdir.test.ts: 521/521 (was 519, +2).

### ADR-0004 §7.2 / §7.4 (T-TMP-38c — first-observed-wins for racing abort → .throw() during cleanup-start, run() surface) — RESOLVED

The .throw()-axis companion to T-TMP-38a (which covered the .return() axis on the same surface, same observation order). Together with T-TMP-38a/T-TMP-38a2 (.return() axis, both observation orders) and T-TMP-38b/T-TMP-38b-run (non-zero-exit axis, both surfaces), this closes SPEC §7.2's "abort concurrent with consumer .return() / .throw()" enumeration on the cleanup-idempotence axis at the cleanup-start window for the abort-first half of the matrix on the run() programmatic surface. The .throw()-first × abort-second inverted-order counterpart is T-TMP-38c2 (now also RESOLVED — see next subsection).

The implementation in `packages/loop-extender/src/run.ts` (`wrapper.throw` lines 419-449) ALREADY conformed: the wrapper.throw() body is symmetric to wrapper.return() — it reads `internalAc.signal.aborted` at entry, only pins `returnCalled` when abort was NOT first-observed, then drives `gen.return(undefined)` so the inner runLoop's try/finally executes cleanupTmpdir. When abort was first-observed, returnCalled stays false, leaving the in-flight wrapper.next() (queued behind the inner gen's finally) free to surface the abort error to the for-await consumer once cleanup completes. CleanupState.attempted gates the single cleanup attempt; the queued gen.return() from the racing .throw() resolves on the now-completed gen with done:true. No implementation change required this iteration — only the test was authored.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 527/527 PASS** — added 2 tests (1 ID × 2 runtimes):
  - **T-TMP-38c**: bash fixture (rule-3 regular-file replacement, identical to T-TMP-38a's fixture). Driver code (run via `runAPIDriver`) creates an `AbortController`, drives `for await (const _ of run("ralph", { signal: ac.signal }))` in an async IIFE that captures any thrown error into `errName` / `errMessage`, polls for the fixture-ready file, calls `ac.abort()` (first observed terminal trigger), polls for the parent-observable cleanup-start marker (existence implies cleanup paused at the seam), then calls `await gen.throw(new Error("test-throw"))` during the bounded pause, then awaits the `iterPromise`. Writes a JSON envelope `{ errName, errMessage }` to stdout. Asserts: (a) `envelope.errName === "AbortError"` AND `envelope.errMessage` does NOT match `/test-throw/` (first-observed abort wins; consumer error silently absorbed per SPEC §9.1 silent-clean-completion contract — load-bearing: a buggy wrapper that surfaced the consumer-thrown error or wrapped it around the abort would shift errName), (b) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line on stderr (no second cleanup attempt — load-bearing: a buggy wrapper that re-entered cleanup on the racing .throw() would emit a second warning), (c) regular-file at the recorded tmpdir path with content `regular-file-replacement` survives.
- Adjacent suites: zero regressions across programmatic-api + env-vars + wfdir + loop-state (590), signals (9). tmpdir.test.ts: 527/527 (was 525, +2).

### ADR-0004 §7.2 / §7.4 / §9.1 / §9.3 (T-TMP-38d3 — first-observed-wins for racing post-final-yield .return() → abort during consumer-return-observed, run() surface) — RESOLVED

`.return()`-axis post-final-yield counterpart on the run() programmatic surface — `.return()` observed FIRST after final yield (post first-`next()`); loopx records the .return() as the first-observed terminal trigger and pauses at the new TEST-SPEC §1.4 `consumer-return-observed` window seam BEFORE dispatching settlement; harness polls the parent-observable pause marker, then calls `ac.abort()` during the bounded pause. Per SPEC §9.3 second paragraph + §9.1 silent-clean-completion-when-no-child rule, the .return() observed earlier wins as the surfaced outcome — clean `{ done: true, value: undefined }` settlement; the racing post-pause abort does NOT displace it. Per SPEC §7.4 idempotence + at-most-one-warning, the racing abort must NOT initiate a second cleanup attempt. Symmetric counterpart to T-TMP-38d2 (the `.throw()`-axis variant on the same observation order, still pending). Together with T-TMP-38d (abort-first × `.throw()`-second post-final-yield) and T-TMP-38d4 (abort-first × `.return()`-second post-final-yield, still pending), this closes one quadrant of the post-final-yield consumer-cancellation × abort matrix on the warning-cardinality axis.

Implementation changes (this iteration introduced new SPEC infrastructure on top of the test):

1. **`packages/loop-extender/src/run.ts` (wrapper.return)** — adds `await maybePauseAtTerminalTriggerWindow("consumer-return-observed")` AFTER the `returnCalled = true` pin (preserving first-observed status recording per the seam's spec'd ordering) and BEFORE `internalAc.abort() / gen.return()` dispatch. The seam call is gated INSIDE the `if (!abortObservedFirst)` branch, so it only fires when .return() is the first-observed terminal trigger — preserves T-TMP-38a's abort-first × .return()-second contract by skipping the seam entirely when abort was observed first (the `surfacePostFinalYieldAbort` branch above also bypasses the seam for the abort-first × post-final-yield case). The seam fires uniformly on any post-first-`next()` `.return()` — both mid-loop and post-final-yield — so the same wiring covers both contexts described by the §1.4 `consumer-return-observed` contract.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 533/533 PASS** — added 4 tests (1 ID × 2 variants × 2 runtimes):
  - **T-TMP-38d3 (stop)**: bash fixture (rule-3 regular-file replacement; `printf '%s' '{"stop":true}'`) with `maxIterations: 5`. Driver code (run via `runAPIDriver`) creates an `AbortController`, calls `await gen.next()` to receive the final yield (verifies `first.value.stop === true`), then calls `gen.return(undefined)` (captures the returned promise into `returnPromise`), polls for the parent-observable consumer-return-observed marker (existence implies wrapper.return paused at the seam), then calls `ac.abort()` during the bounded pause, awaits `returnPromise` (capturing `settled.done` and `settled.value`). Writes JSON envelope `{ errName, errMessage, returnDone, returnValue, firstStop, firstDone }`.
  - **T-TMP-38d3 (maxIterations)**: same fixture structure but `printf '%s' '{}'` (raw fallback `{ result: "{}" }`) with `maxIterations: 1` (loopx-driven iteration-count-limit final yield). Verifies `first.done === false` to confirm the harness entered the post-final-yield window.
  - Asserts (both variants): (a) `envelope.returnDone === true` AND `envelope.returnValue === null` AND `envelope.errName === ""` AND `errMessage` does NOT match `/abort/i` (clean settlement, no abort surfaced — first-observed .return() silently absorbs the racing abort per SPEC §9.1 consumer-cancellation contract; load-bearing: a buggy wrapper that surfaced the abort error would shift returnDone / errName), (b) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line on stderr (per-run cardinality holds across racing triggers — `CleanupState.attempted` deduplicates; load-bearing: a buggy implementation that started a second cleanup attempt on the racing abort would emit a second warning), (c) rule-3 regular-file replacement at the recorded path survives (cleanup completed once and left the file in place per SPEC 7.4 rule-3; load-bearing: a buggy implementation that re-touched the rule-3 entry on the second attempt would corrupt or remove the file).
- Adjacent suites: zero regressions across programmatic-api + signals + loop-state + execution (544/544), unit + harness (158/158).

### ADR-0004 §7.2 / §7.4 (T-TMP-38c2 — first-observed-wins for racing .throw() → abort during consumer-throw-observed, run() surface, inverted-order parity) — RESOLVED

Inverted-order parity counterpart to T-TMP-38c on the run() programmatic surface — .throw() observed FIRST mid-loop (post first-next()); loopx records the .throw() as the first-observed terminal trigger and pauses at the new TEST-SPEC §1.4 `consumer-throw-observed` window seam BEFORE dispatching the resulting silent settlement; harness polls the parent-observable pause marker, then calls `ac.abort()` during the bounded pause. Per SPEC §7.2 first-observed-wins, the .throw() observed earlier wins as the surfaced outcome — the racing abort must NOT displace the silent-clean-completion settlement (SPEC §9.1) and must NOT initiate a second cleanup attempt (SPEC §7.4 idempotence + at-most-one-warning).

Implementation changes (this iteration introduced new SPEC infrastructure — not just test authoring):

1. **`packages/loop-extender/src/test-seams.ts` (new)** — extracts the marker-write + bounded-sleep mechanic into a shared `maybePauseAtTerminalTriggerWindow(expectedWindow: string)` helper. When `NODE_ENV=test` and `LOOPX_TEST_TERMINAL_TRIGGER_PAUSE` equals `expectedWindow`, writes a UTF-8 JSON marker file (when `LOOPX_TEST_TERMINAL_TRIGGER_PAUSE_MARKER` is set) using `openSync` / `writeSync` / `fsyncSync` / `closeSync` BEFORE the bounded delay begins (per TEST-SPEC §1.4 ordering — marker visibility implies pause is about to start). Bounded delay: 3 seconds (within the spec-required [2s, 10s] range). All other `LOOPX_TEST_TERMINAL_TRIGGER_PAUSE` window values not yet wired in implementation are no-ops at non-matching callsites (e.g., setting it to `abort-listener` will still be a no-op since the abort-listener callsite doesn't yet invoke this helper).
2. **`packages/loop-extender/src/tmpdir.ts` (refactored)** — removed local `maybePauseAtCleanupStart` and `asyncSleep`; `cleanupTmpdir` now invokes `await maybePauseAtTerminalTriggerWindow("cleanup-start")` from the shared helper. Same delay, same marker contract, same NODE_ENV gating — pure refactor with no behavior change. Verified: full tmpdir.test.ts 529/529 pass (existing T-TMP-38/38a/38a2/38b/38b-run/38c/38f/39 cleanup-start window tests all still pass — proves the refactor is behavior-preserving).
3. **`packages/loop-extender/src/run.ts` (wrapper.throw)** — adds `await maybePauseAtTerminalTriggerWindow("consumer-throw-observed")` AFTER the `returnCalled = true` pin (preserving first-observed status recording per the seam's spec'd ordering) and BEFORE `internalAc.abort() / gen.return()` dispatch. The seam call is gated INSIDE the `if (!abortObservedFirst)` branch, so it only fires when .throw() is the first-observed terminal trigger. This preserves T-TMP-38c's abort-first × .throw()-second contract: when abort was observed first, the `!abortObservedFirst` branch is skipped entirely (returnCalled stays false, no pause, dispatch proceeds immediately) — the existing wrapper.throw flow is unchanged for the abort-first half of the matrix.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 529/529 PASS** — added 2 tests (1 ID × 2 runtimes):
  - **T-TMP-38c2**: bash fixture (rule-3 regular-file replacement, identical to T-TMP-38c's fixture). Driver code (run via `runAPIDriver`) creates an `AbortController`, drives `for await (const _ of run("ralph", { signal: ac.signal }))` in an async IIFE that captures any thrown error into `errName` / `errMessage`, polls for the fixture-ready file, calls `gen.throw(new Error("test-throw"))` (first observed terminal trigger — captures the returned promise into `throwPromise`), polls for the parent-observable consumer-throw-observed marker (existence implies wrapper.throw paused at the seam), then calls `ac.abort()` during the bounded pause, awaits `throwPromise` (capturing `settled.done` and `settled.value`), then awaits the `iterPromise`. Writes JSON envelope `{ errName, errMessage, throwDone, throwValue }`. Asserts: (a) `envelope.throwDone === true` AND `envelope.throwValue === null` AND `envelope.errName === ""` AND `envelope.errMessage` does NOT match `/abort/i` AND `envelope.errMessage` does NOT match `/test-throw/` (clean settlement, no abort surfaced — first-observed .throw() silently absorbs both itself and the racing abort per SPEC §9.1 consumer-cancellation contract; load-bearing: a buggy wrapper that surfaced the abort error to the for-await loop or returned the consumer error to the gen.throw() promise would shift these), (b) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line on stderr (per-run cardinality holds across racing triggers — `CleanupState.attempted` deduplicates; load-bearing: a buggy implementation that started a second cleanup attempt on the racing abort would emit a second warning), (c) rule-3 regular-file replacement at the recorded path survives.
- Adjacent suites: zero regressions. tmpdir.test.ts: 529/529 (was 527, +2). programmatic-api + env-vars + wfdir + loop-state + execution: 635/635 across 5 e2e files. signals: 9/9. unit + harness: 158/158.

### ADR-0004 §7.2 / §7.4 / §9.1 / §9.3 (T-TMP-38d2 — first-observed-wins for racing post-final-yield .throw() → abort during consumer-throw-observed, run() surface) — RESOLVED

`.throw()`-axis post-final-yield counterpart on the run() programmatic surface — `.throw()` observed FIRST after final yield (post first-`next()`); loopx records the .throw() as the first-observed terminal trigger and pauses at the TEST-SPEC §1.4 `consumer-throw-observed` window seam BEFORE dispatching settlement; harness polls the parent-observable pause marker, then calls `ac.abort()` during the bounded pause. Per SPEC §9.3 second paragraph + §9.1 silent-clean-completion-when-no-child rule, the .throw() observed earlier wins as the surfaced outcome — clean `{ done: true, value: undefined }` settlement; the consumer-supplied error is silently absorbed AND the racing post-pause abort does NOT displace the settlement. Per SPEC §7.4 idempotence + at-most-one-warning, the racing abort must NOT initiate a second cleanup attempt. Symmetric counterpart to T-TMP-38d3 (the `.return()`-axis variant on the same observation order). Together with T-TMP-38d (abort-first × `.throw()`-second post-final-yield, still pending) and T-TMP-38d4 (abort-first × `.return()`-second post-final-yield, still pending), this closes one quadrant of the post-final-yield consumer-cancellation × abort matrix on the warning-cardinality axis.

Implementation status: NO implementation changes required this iteration — only the test was authored. The wrapper.throw seam wiring landed in the T-TMP-38c2 iteration: at `packages/loop-extender/src/run.ts` line 461, `await maybePauseAtTerminalTriggerWindow("consumer-throw-observed")` is called inside the `if (!abortObservedFirst)` branch AFTER pinning `returnCalled = true`. Critically, the seam call is positioned to fire on **any** post-first-`next()` `.throw()` — both mid-loop AND post-final-yield — uniformly: when post-final-yield is true and abort is NOT yet observed, the wrapper.throw flow goes:
1. `if (settled)` → false (settled=false)
2. `if (postFinalYield && internalAc.signal.aborted && !returnCalled)` → false (abort not observed) → skip surfacePostFinalYieldAbort branch
3. `abortObservedFirst = false`; `if (!abortObservedFirst)` → true → returnCalled=true; seam fires
4. During seam pause: `ac.abort()` arrives → user signal listener fires `internalAc.abort()` (just sets the flag)
5. Seam ends (3s)
6. `internalAc.abort()` (no-op, already aborted); `gen.return(undefined)` drives inner gen settlement
7. Inner gen's try/finally runs cleanupTmpdir ONCE (CleanupState.attempted gates idempotence)
8. wrapper.throw returns `{ done: true, value: undefined }` — clean silent settlement
9. The consumer-supplied "test-throw" error is absorbed (per SPEC §9.1 consumer-cancellation contract); the racing abort error is also absorbed (per SPEC §9.3 second paragraph + §7.2 first-observed-wins residual rule)

So the existing seam wiring naturally satisfies T-TMP-38d2 — fix_plan.md's earlier note "the wrapper.throw post-final-yield path goes through surfacePostFinalYieldAbort, not the abort-observation pinning logic" was outdated (it predated the post-final-yield × abort-NOT-observed branch's natural fall-through to the seam).

Now-passing tests (this iteration):
- **tmpdir.test.ts: 537/537 PASS** — added 4 tests (1 ID × 2 variants × 2 runtimes):
  - **T-TMP-38d2 (stop)**: bash fixture (rule-3 regular-file replacement; `printf '%s' '{"stop":true}'`) with `maxIterations: 5`. Driver code (run via `runAPIDriver`) creates an `AbortController`, calls `await gen.next()` to receive the final yield (verifies `first.value.stop === true`), then calls `gen.throw(new Error("test-throw"))` (captures the returned promise into `throwPromise`), polls for the parent-observable consumer-throw-observed marker (existence implies wrapper.throw paused at the seam), then calls `ac.abort()` during the bounded pause, awaits `throwPromise` (capturing `settled.done` and `settled.value`). Writes JSON envelope `{ errName, errMessage, throwDone, throwValue, firstStop, firstDone }`.
  - **T-TMP-38d2 (maxIterations)**: same fixture structure but `printf '%s' '{}'` (raw fallback `{ result: "{}" }`) with `maxIterations: 1` (loopx-driven iteration-count-limit final yield). Verifies `first.done === false` to confirm the harness entered the post-final-yield window.
  - Asserts (both variants): (a) `envelope.throwDone === true` AND `envelope.throwValue === null` AND `envelope.errName === ""` AND `errMessage` does NOT match `/abort/i` AND `errMessage` does NOT match `/test-throw/` (clean settlement, no abort or consumer-thrown error surfaced — first-observed .throw() silently absorbs both itself and the racing abort per SPEC §9.1 consumer-cancellation contract; load-bearing: a buggy wrapper that surfaced either error would shift these), (b) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line on stderr (per-run cardinality holds across racing triggers — `CleanupState.attempted` deduplicates; load-bearing: a buggy implementation that started a second cleanup attempt on the racing abort would emit a second warning), (c) rule-3 regular-file replacement at the recorded path survives (cleanup completed once and left the file in place per SPEC 7.4 rule-3; load-bearing: a buggy implementation that re-touched the rule-3 entry on the second attempt would corrupt or remove the file).
- Adjacent suites: zero regressions across programmatic-api + signals + loop-state + execution (499/499), unit + harness (158/158).

### ADR-0004 §7.2 / §7.4 (T-TMP-38b2 / T-TMP-38b2-run — abort-listener seam wiring + first-observed-wins under racing abort → non-zero-exit during abort-listener) — RESOLVED

Inverted-order parity counterpart to T-TMP-38b / T-TMP-38b-run on the runPromise() and run() programmatic surfaces. Where T-TMP-38b/-run covered iteration-error first × abort second (`cleanup-start` window), T-TMP-38b2/-run cover the abort first × non-zero-script-exit second race at the new TEST-SPEC §1.4 `abort-listener` window. The seam fires AFTER abort is recorded as first-observed (the user-signal listener's flag pin) but BEFORE loopx dispatches the abort outcome (i.e., before kicking off child termination, abortPromise rejection, or abort-error surfacing). This keeps the active child alive during the bounded ~3s pause (TEST-SPEC §1.4 form (a)), so the harness can race a child-driven non-zero exit (SIGUSR1 → bash trap → `exit 1`) against the still-paused abort dispatch on the still-alive process group.

This iteration introduces fundamentally new SPEC §7.2 infrastructure that decouples first-observed-trigger determination from `internalAc.signal.aborted` (which the new seam delays). Without this decoupling, the wrapper's first-observed checks would see `internalAc.signal.aborted === false` during the seam-paused dispatch and incorrectly classify the racing trigger as first-observed. The implementation:

1. **`packages/loop-extender/src/loop.ts`** — added `FirstObservedTrigger` type alias (`"abort" | "iteration" | "consumer" | null`) and `FirstObservedRef` interface holding a single `trigger` slot, both exported. Added `firstObservedRef?: FirstObservedRef` to `LoopOptions`. Added a `pinIterationFirstObserved()` helper that conditionally sets `firstObservedRef.trigger = "iteration"` (only if currently null). Each iteration-level throw now calls the helper before throwing: the script-failure branch (`if (result.exitCode !== 0)`), the parse-goto branch (`if (!goto.ok)`), the bare-goto unknown-script branch, the workflow-qualified-goto unknown-workflow branch, and the workflow-qualified-goto unknown-script branch.

2. **`packages/loop-extender/src/run.ts`** — added a `firstObservedRef = { trigger: null }` slot per run() call, threaded through to `runInternal` → `runLoop` via the new field. The user-signal listener (`onUserSignalAbort`) now: (a) pins `firstObservedRef.trigger = "abort"` if currently null AT ENTRY (before any pause); (b) dispatches `internalAc.abort()` synchronously when not in test mode OR when `LOOPX_TEST_TERMINAL_TRIGGER_PAUSE !== "abort-listener"`; (c) when the abort-listener seam is active, schedules an async IIFE that awaits `maybePauseAtTerminalTriggerWindow("abort-listener")` and only THEN calls `internalAc.abort()`. The eager-aborted-at-call-time branch (`if (snap.signal.aborted)` at the top of run()'s signal wiring) ALSO pins `firstObservedRef.trigger = "abort"` since the listener never fires there.

3. **`packages/loop-extender/src/run.ts` (wrapper)** — every previous `internalAc.signal.aborted` first-observed check now consults `firstObservedRef.trigger === "abort"` instead. Touched callsites: `wrapper.next()` post-final-yield abort branch (line ~398), `wrapper.return()` post-final-yield abort branch and mid-loop `abortObservedFirst` check (lines ~451/466), `wrapper.throw()` mirror branches (lines ~519/534), and `[Symbol.asyncDispose]` (line ~573). When consumer cancellation acts as first-observed (the `!abortObservedFirst` branch in .return() / .throw() / asyncDispose), the wrapper now ALSO pins `firstObservedRef.trigger = "consumer"` if currently null, so a subsequent abort-listener firing sees the slot occupied and does not displace the consumer-cancellation outcome.

4. **`packages/loop-extender/src/run.ts` (wrapper.next() catch)** — added a NEW first-observed-wins gate AFTER the existing `if (returnCalled) return done:true` clause: when an iteration-level error reaches the wrapper.next() catch BUT abort had already been pinned as first-observed by the listener, the wrapper now throws `makeAbortError(snap.signal ?? internalAc.signal)` instead of the iteration error. This is the load-bearing change that produces the abort-error surfacing under T-TMP-38b2 / T-TMP-38b2-run when the SIGUSR1-driven script-failure reached runLoop first. The pre-existing path (no abort first-observed) still surfaces the original `err` unchanged, preserving T-TMP-38b / T-TMP-38b-run semantics (where iteration-error was actually observed first via runLoop's `pinIterationFirstObserved` call BEFORE the racing abort-during-cleanup-start).

Now-passing tests (this iteration):
- **tmpdir.test.ts: 541/541 PASS** — added 4 tests (1 ID × 2 surfaces × 2 runtimes):
  - **T-TMP-38b2** (runPromise surface): bash fixture installs `trap 'exit 1' USR1`, writes its own PID via `printf '%s' "$$" > "$PID_MARKER_PATH"`, replaces the tmpdir with the rule-3 regular-file replacement, then `sleep 30 & wait`s. Driver code (run via `runAPIDriver`) creates an `AbortController`, kicks off `runPromise("ralph", { signal: ac.signal, maxIterations: 1 })`, polls for the fixture-ready file, calls `ac.abort()` (FIRST observed terminal trigger — the listener pins `firstObservedRef.trigger = "abort"` AT ENTRY and schedules the abort-listener pause), polls for the parent-observable abort-listener marker (existence implies the seam paused at entry to the dispatch path), reads the script's PID from the PID marker, sends `SIGUSR1` to the script's process group (`process.kill(-pid, "SIGUSR1")`) so the racing iteration error fires while loopx is still paused. Awaits the promise inside try/catch capturing `errName` / `errMessage`. Writes JSON envelope `{ errName, errMessage }`. Asserts: (a) `envelope.errName === "AbortError"` AND `envelope.errMessage` does NOT match `/exited with code/` (load-bearing — a buggy wrapper.next() catch that surfaced the iteration error directly without consulting `firstObservedRef` would shift this), (b) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line on stderr (per-run cardinality holds across racing triggers — `CleanupState.attempted` deduplicates; load-bearing: a buggy implementation that started a second cleanup attempt on the racing iteration error would emit a second warning), (c) regular-file at the recorded tmpdir path with content `regular-file-replacement` survives.
  - **T-TMP-38b2-run** (run surface): same fixture, driver code drives `for await (const _ of run("ralph", { signal: ac.signal, maxIterations: 1 }))` in an async IIFE that captures any thrown error into `errName` / `errMessage`, otherwise mirrors the runPromise variant (poll fixture-ready, abort, poll pause marker, send SIGUSR1, await iterPromise). Asserts: same shape as T-TMP-38b2 — verifies that a buggy implementation that wired the abort-listener seam correctly on `runPromise` rejection but routed `run()` settlement through a different first-observed dispatcher would fail this counterpart.
- Adjacent suites: zero regressions across programmatic-api + env-vars + wfdir + loop-state + execution (635/635), signals (9/9), unit + harness (158/158). Full tmpdir.test.ts run: 541/541 in ~155s.

### ADR-0004 §7.4 (T-TMP-42b — cleanup-fault warning does not mask abort terminal × programmatic surfaces) — RESOLVED

SPEC §7.4 final paragraph: "warnings do not affect the CLI exit code, the generator outcome, or the promise rejection reason." The cleanup-warning-does-not-affect-outcome contract holds for both the cleanup-safety warning branch and the cleanup-failure warning branch on every terminal type. Coverage matrix entries previously pinned: T-TMP-35g/35h (cleanup-safety × abort × programmatic surfaces), T-TMP-40/41/42 + 40a/40b/41a/41b/42d/42e (cleanup-failure × success × all three surfaces), T-TMP-42a (cleanup-failure × script-error × all three surfaces), T-TMP-42c (cleanup-failure × CLI signal terminal). T-TMP-42b closes the remaining missing diagonal: cleanup-failure warning × abort terminal × programmatic surfaces (abort is programmatic-only). A buggy implementation that promoted the cleanup-failure warning to a cleanup-failure-class rejection in place of the abort error — an implementation-defect mode that would still pass the cleanup-SAFETY counterpart (T-TMP-35g/35h) — would fail T-TMP-42b's first assertion.

The implementation in `runLoop` (`packages/loop-extender/src/loop.ts:120-217`) and the `run.ts` wrapper / signal-listener wiring (`packages/loop-extender/src/run.ts:307-348`) already conformed: the iteration body wraps in `try/finally` so `cleanupTmpdir(tmpdirResource, cleanupState)` runs before any abort error propagates out of the generator. The cleanup-fault seam (`LOOPX_TEST_CLEANUP_FAULT={lstat-fail, symlink-unlink-fail, recursive-remove-fail}` in `tmpdir.ts:184-279`) fires inside `cleanupTmpdir` and emits via `emitCleanupWarning` which writes to stderr but never rewrites the in-flight throw value.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 553/553 PASS** — added 12 tests (3 cleanup-fault seams × 2 programmatic surfaces × 2 runtimes):
  - **T-TMP-42b (lstat-fail / symlink-unlink-fail / recursive-remove-fail) [runPromise]**: bash fixture observes `LOOPX_TMPDIR` into an external marker, performs the seam-specific path manipulation if needed (for `symlink-unlink-fail`: `rm -rf "$LOOPX_TMPDIR" && ln -s /tmp "$LOOPX_TMPDIR"` so dispatch reaches rule 2; for `recursive-remove-fail`: writes a few files inside the tmpdir so dispatch reaches rule 4 on a non-empty identity-matched directory; for `lstat-fail`: no path manipulation, the seam fires on the top-level `lstat`), writes its PID to a separate marker, then blocks indefinitely. Driver code (run via `runAPIDriver`) creates an `AbortController`, kicks off `runPromise("ralph", { signal: c.signal })`, polls for both markers, calls `c.abort()` from the harness, awaits the rejection capturing `errName` / `errMessage`, then waits up to 10s for the script's PID to exit (probed via `process.kill(pid, 0)` returning ESRCH). Asserts: (a) promise rejected with abort error (`errName === "AbortError"` or `/abort/i.test(errMsg)`) and message does NOT contain "cleanup" — load-bearing — a buggy implementation that promoted the cleanup-failure warning to a cleanup-failure-class rejection in place of the abort error would shift `errName` and add "cleanup" to the message, (b) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line on stderr (per-run cleanup-warning cardinality holds under the abort terminal × cleanup-failure branch), (c) child PG terminated per SPEC §9.1 / §9.5 — abort-driven process-group termination still applies under the cleanup-failure branch, (d) path persistence per fault seam (lstat-fail: identity-matched directory remains; symlink-unlink-fail: symlink remains; recursive-remove-fail: directory remains).
  - **T-TMP-42b (lstat-fail / symlink-unlink-fail / recursive-remove-fail) [run]**: same fixture, driver code creates an `AbortController` and drives `const gen = run("ralph", { signal: c.signal }); const nextP = gen.next();` (`nextP.catch(() => {})` to avoid unhandled rejection) then polls for both markers, calls `c.abort()`, awaits `nextP` capturing `thrown` / `errName` / `errMessage`. Same assertion shape as runPromise variant. Verifies a buggy implementation that wired the cleanup-failure-warning-does-not-affect-outcome contract correctly on `runPromise` rejection but routed `run()` settlement through a different cleanup-dispatcher would fail this counterpart.
- Adjacent suites: zero regressions across programmatic-api + env-vars + wfdir + loop-state + execution (635/635), signals (9/9), unit + harness (158/158). Full tmpdir.test.ts run: 553/553 in ~157s.

### ADR-0004 §7.2 / §7.4 / §9.3 (T-TMP-38d / T-TMP-38d4 — first-observed-wins for racing post-final-yield abort → .throw() / .return() during abort-listener) — RESOLVED

Closing two of the four post-final-yield consumer-cancellation × abort race quadrants on the warning-cardinality axis. T-TMP-38d (the `.throw()` axis) and T-TMP-38d4 (the `.return()` axis) cover the abort-FIRST observation order; T-TMP-38d2 (`.throw()`-first × abort-second) and T-TMP-38d3 (`.return()`-first × abort-second) cover the inverted order. Together the four IDs close the full {`.return()` / `.throw()`} × {abort-first / consumer-first} 2×2 matrix on the post-final-yield surface.

Per SPEC §9.3's abort-after-final-yield rule, when abort is observed first after the final yield (before generator settlement), the abort outcome displaces normal completion regardless of which consumer cancellation later races against it. Equivalent contract to T-API-66b / T-API-66e (abort-first × `.return()` / `.throw()` post-final-yield outcome-axis tests), but driven through the `abort-listener` pause race rather than direct interaction sequencing — pinning the warning-cardinality axis on the same race.

The implementation in `packages/loop-extender/src/run.ts` ALREADY conformed: when `firstObservedRef.trigger === "abort"` is set (which the user-signal listener pins at entry, BEFORE the abort-listener seam pause begins), the wrapper's `.return()` / `.throw()` post-final-yield branch (lines 397-404 / 450-458 / 518-526) jumps to `surfacePostFinalYieldAbort` synchronously, drives the inner gen's finally (cleanup runs once with rule-3 warning), and throws AbortError. The abort-listener seam itself was wired in the T-TMP-38b2 iteration; this iteration verifies that the post-final-yield code paths participate correctly.

The `abort-listener` seam (rather than `cleanup-start` or `consumer-throw-observed`) is essential here per TEST-SPEC §1.4: SPEC §7.4 does NOT forbid an opportunistic implementation from running cleanup after the final yield but before settlement, so a `cleanup-start` pause cannot reliably coordinate this race. The `abort-listener` seam fires deterministically when `c.abort()` is delivered, regardless of whether the implementation had also opportunistically begun cleanup before the abort arrived.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 561/561 PASS** — added 8 tests (2 IDs × 2 variants × 2 runtimes):
  - **T-TMP-38d (stop) / T-TMP-38d (maxIterations)**: bash fixture (rule-3 regular-file replacement) emits final yield then exits 0. Variant (stop) emits `{"stop":true}` + `maxIterations: 5`; variant (maxIterations) emits raw `{}` (parses as `{ result: "{}" }` per SPEC §2.3 raw-fallback) + `maxIterations: 1`. Driver code (run via `runAPIDriver`) creates an `AbortController`, calls `await gen.next()` to consume the final yield (asserts `first.value.stop === true` for stop variant or `first.done === false` for maxIterations variant), calls `ac.abort()` FIRST (pins `firstObservedRef.trigger = "abort"` + schedules abort-listener seam pause), polls the parent-observable abort-listener marker, then calls `gen.throw(new Error("test-throw"))` during the bounded pause, awaits the throwPromise (captures rejection in try/catch). Asserts: (a) `envelope.errName === "AbortError"` AND `envelope.errMessage` does NOT match `/test-throw/` AND `envelope.throwDone === false` (load-bearing: a buggy wrapper that surfaced the consumer-thrown error or a clean settlement would shift these); (b) exactly one `LOOPX_TEST_CLEANUP_WARNING\t…` line on stderr; (c) rule-3 regular-file replacement at the recorded path survives.
  - **T-TMP-38d4 (stop) / T-TMP-38d4 (maxIterations)**: same fixture, same observation order. Driver code calls `gen.return(undefined)` instead of `gen.throw(...)` during the abort-listener pause, awaits the returnPromise. Asserts: (a) `envelope.errName === "AbortError"` AND `envelope.returnDone === false` (load-bearing: a buggy wrapper that surfaced a clean `.return()` settlement would shift these); (b) exactly one cleanup warning; (c) rule-3 file persists.
- Adjacent suites: zero regressions across programmatic-api + env-vars + wfdir + loop-state + execution (635/635), signals (9/9), unit + harness + typecheck (172/172). Full tmpdir.test.ts run: 561/561 in ~182s.

### ADR-0004 §7.2 / §7.4 (T-TMP-38e / T-TMP-38e-run — child-spawn-failure × abort cleanup idempotence on programmatic surfaces) — RESOLVED

Closing the child-spawn-failure × abort race quadrants on the cleanup-idempotence + warning-cardinality axes. Two race configurations are exercised: variant (a) the spawn failure is observed FIRST (script-failure shape iteration outcome wins, racing abort does not displace), variant (b) the abort is observed FIRST (abort outcome wins, racing post-abort spawn failure does not displace). Both variants are run on both programmatic surfaces (`runPromise()` and `run()`) on both runtimes (node, bun) — 2 IDs × 2 variants × 2 runtimes = 8 tests.

Implementation iteration. Two new TEST-SPEC §1.4 seam windows wired in `packages/loop-extender/src/execution.ts`:
- **`child-spawn-attempt`**: pause window placed BEFORE the `spawn()` call. Used by variant (b) — the harness aborts during this pause window, BEFORE any spawn syscall has executed, so the abort can be pinned as first-observed.
- **`child-spawn-failure`**: pause window placed AFTER catching the synchronous spawn rejection (NUL-byte-in-env triggers a synchronous throw from `child_process.spawn` because Node validates env-pair byte content before invoking the syscall) AND alongside the async `'error'` event handler for spawn failures discovered after fork. Used by variant (a) — `firstObservedRef.trigger` is pinned to `"iteration"` BEFORE entering the seam pause so a racing abort delivered during the pause cannot displace the spawn-failure outcome.

Two new fields threaded through `ExecOptions`: `firstObservedRef?: FirstObservedRef` (the same shared trigger-tracking slot already used by `runLoop` and `run.ts`'s wrapper), so `executeScript` can pin `trigger = "iteration"` when null at spawn-failure observation. Both the synchronous spawn throw (NUL-in-env path) and the async `'error'` event handler pin first-observed BEFORE the seam pause runs, so any racing abort delivered during the bounded pause cannot retroactively claim first-observed.

A new gate added to `runLoop`'s `abortPromise` in `loop.ts`: when `firstObservedRef.trigger === "iteration"`, the abort listener does NOT reject the abortPromise that participates in the per-iteration `Promise.race`. This is load-bearing — without the gate, a racing abort observed AFTER the spawn-failure was pinned as iteration-first would still reject the abortPromise, and the race in `runLoop` would resolve to the abort-error rejection rather than letting the iteration error propagate as the surfaced terminal outcome. The gate preserves the spawn-failure-first contract by ensuring the iteration error is what reaches the wrapper's catch.

A synthetic `onAbort` dispatch is performed at child-listener-attach time when `signal.aborted` is already true: this ensures variant (b) — where the abort fired during the `child-spawn-attempt` pause BEFORE the spawn() call — still terminates the active child if `spawn()` succeeded after the pause resumed. Without the synthetic dispatch, the abort listener attached AFTER spawn would never fire, leaving the just-spawned child unkilled.

Cleanup-warning is induced via `LOOPX_TEST_CLEANUP_FAULT=recursive-remove-fail` so rule-4 cleanup (the empty tmpdir loopx created) emits the SPEC §7.4 warning. Without this fault, cleanup of the empty tmpdir succeeds warning-free, and the cardinality assertion (`exactly one cleanup warning`) cannot distinguish a single-attempt from a double-attempt code path. The cleanup-fault seam reliably emits one warning per `cleanupTmpdir` invocation, so `count === 1` proves cleanup ran exactly once across the racing trigger pair (single-attempt code path) while `count === 2+` would catch a regression where each trigger independently invoked cleanup.

Now-passing tests (this iteration):
- **tmpdir.test.ts: 569/569 PASS** — added 8 tests (2 IDs × 2 variants × 2 runtimes):
  - **T-TMP-38e (variant a, variant b) [runPromise]**: variant (a) sets `LOOPX_TEST_TERMINAL_TRIGGER_PAUSE=child-spawn-failure` + a NUL-in-env value, awaits `runPromise(...)` with a signal whose `c.abort()` is delivered during the pause; asserts the rejection has script-failure shape (NOT AbortError) and exactly one cleanup warning, tmpdir cleaned once. Variant (b) sets `LOOPX_TEST_TERMINAL_TRIGGER_PAUSE=child-spawn-attempt`, calls `c.abort()` during the spawn-attempt pause BEFORE spawn fires; asserts AbortError rejection and exactly one cleanup warning.
  - **T-TMP-38e-run (variant a, variant b) [run]**: same race configurations driven through the `run()` generator surface, with `gen.next().catch(() => {})` to defer rejection observation, polling for the parent-observable pause marker, then `c.abort()`. Same assertion shape per variant.
- Adjacent suites: zero regressions across programmatic-api + env-vars + wfdir + loop-state + execution (635/635 e2e), signals (9/9), unit + harness (158/158).

### ADR-0004 §9.3 / §9.1 (T-API-66/66a/66b/66c/66d/66e — abort-after-final-yield outcome-axis on the API surface) — RESOLVED

SPEC §9.3 second paragraph: "Abort observed after the final yield but before the generator settles produces the abort error on the next generator interaction — `g.next()`, `.return()`, or `.throw()`." The "final yield" is triggered by either `maxIterations`-reached OR a script-emitted `stop: true`; the rule applies symmetrically across both triggers and across all three settle-triggering interactions. For `.throw()`, the abort error displaces the consumer-supplied error per SPEC §9.3 (signal wins).

The cleanup-ORDERING-axis tests (T-TMP-23/24a/24c/24d/24e/24f/24g in tmpdir.test.ts) already pin the contract that tmpdir is removed BEFORE the abort error surfaces. The OUTCOME axis (the abort error is the surfaced rejection at all — vs silent `{ done: true }` settlement, and the abort error displaces a concurrent consumer-supplied error in `.throw()`) was unpinned on the API surface until this iteration. A buggy implementation that ran cleanup correctly but then resolved silently `{ done: true }` on the post-final-yield interaction (or that allowed `.throw()`'s consumer-error to surface in place of the abort error) would pass the T-TMP series yet fail T-API-66/66a/66b/66c/66d/66e. The two families are complementary — neither subsumes the other.

The implementation in `packages/loop-extender/src/run.ts` already conformed: each of `wrapper.next()` (line 398-403), `wrapper.return()` (line 451-457), and `wrapper.throw()` (line 519-525) carries a `if (postFinalYield && firstObservedRef.trigger === "abort" && ...) await surfacePostFinalYieldAbort();` branch that drives `gen.return(undefined)` to run the inner runLoop's try/finally (cleanup) before throwing `makeAbortError(...)`. The `firstObservedRef.trigger === "abort"` check (introduced for T-TMP-38b2 to decouple first-observed determination from `internalAc.signal.aborted`) is pinned by the user-signal listener at entry, so a `c.abort()` call before the second interaction sets it synchronously. Test-only iteration; no implementation changes required.

Now-passing tests (this iteration):
- **programmatic-api.test.ts: 370/370 PASS** — added 12 tests (6 IDs × 2 runtimes) under a new `SPEC: Abort After Final Yield` describe block:
  - **T-API-66 (`.next()`, maxIterations: 1)**: bash fixture emits `{"result":"ok"}`. Driver: `await gen.next()` (final yield), `c.abort()`, `await gen.next()` → expect rejection. Asserts (a) `firstDone === false`, (b) `firstHasValue === true`, (c) result is rejection with `name === "AbortError" || /abort/i.test(msg)` (load-bearing — silent `{ done: true }` would shift `result.kind` to `"resolved"`).
  - **T-API-66a (`.return()`, maxIterations: 1)**: same fixture; driver swaps `gen.return(undefined)` for the second interaction. Asserts rejection with abort-error shape (load-bearing — pre-§9.3 wrapper would have settled `{ done: true, value: undefined }` silently per the consumer-cancellation contract).
  - **T-API-66b (`.throw()`, maxIterations: 1)**: same fixture; driver swaps `gen.throw(new Error("consumer-err"))`. Asserts rejection with abort-error shape AND `msg !== "consumer-err"` (load-bearing — abort displaces consumer-supplied error per SPEC §9.3 final paragraph).
  - **T-API-66c (`.next()`, stop:true / maxIterations: 5)**: bash fixture emits `{"stop":true}`. Driver verifies `first.value.stop === true` then aborts and calls `.next()`. Asserts rejection with abort-error shape (load-bearing — a buggy implementation that recognized "final yield" only via `maxIterations`-reached and treated `stop:true` as a separate "settle silently" path that bypassed abort-precedence would silently resolve `{ done: true }` and fail).
  - **T-API-66d (`.return()`, stop:true / maxIterations: 5)**: same fixture; `.return()` axis. Asserts rejection.
  - **T-API-66e (`.throw()`, stop:true / maxIterations: 5)**: same fixture; `.throw()` axis with consumer-supplied error. Asserts rejection AND `msg !== "consumer-err"`.
- Adjacent suites: zero regressions across tmpdir (561), env-vars (96), wfdir (40), signals (9), loop-state (96), execution (45), unit (143), harness (15), typecheck OK. Full programmatic-api.test.ts run: 370/370 in ~47s.

### ADR-0004 §9.5 / §8.3 (T-API-50/50a/50b/50c/50d/50e/50h — RunOptions.env basic injection across iteration / goto / loop-reset / value-shape contract) — RESOLVED

SPEC §9.5 / §8.3 contract: `RunOptions.env` entries are injected into every spawned script's environment, applying uniformly to the starting target, intra- and cross-workflow `goto` destinations, and loop resets. Entries merge in tier 2 of §8.3's precedence chain (after global env file, after local env file, after inherited `process.env`; before protocol-tier `LOOPX_*` vars). The contract has been load-bearing for the §9.5 RunOptions.env-tier-2-env-merging implementation (now in `run.ts` + `types.ts`) since that landed; T-API-50 series pins the externally-observable end-to-end contract on the API surface that the implementation actually reaches every spawn point and propagates the value byte-for-byte.

The implementation in `packages/loop-extender/src/run.ts` (`snapshotEnv` + `runInternal` env merging) and `packages/loop-extender/src/execution.ts` (env injection at spawn time) ALREADY conformed: `mergeEnv(globalEnv, localEnv)` overlaid with `snap.env` then protocol vars at execution time produces the spec'd precedence; the merged env is computed once per call (eager-snapshot under `runPromise`, lazy under `run`) and passed to every `executeScript` invocation regardless of which workflow / script the iteration targets. Test-only iteration; no implementation changes required.

Now-passing tests (this iteration):
- **programmatic-api.test.ts: 384/384 PASS** — added 14 tests (7 IDs × 2 runtimes) under a new `SPEC: RunOptions.env Basic Injection` describe block:
  - **T-API-50 (runPromise)**: bash fixture writes `present\\t<value>` or `absent` to a marker depending on whether `MYVAR` is set (using `${MYVAR+x}` parameter expansion to distinguish set-but-empty from unset). Driver `runPromise("ralph", { env: { MYVAR: "hello" }, maxIterations: 1 })`. Asserts (a) one Output, (b) marker = `present\\thello` (load-bearing — `absent` would catch a regression where the env never propagated; `present\\t<wrong>` would catch a regression where the value was mangled).
  - **T-API-50a (run, multi-iteration)**: bash fixture increments a counter, writes `iter<N>.txt` with `MYVAR` value, stops after 2 iterations. Driver drives `for await` with `maxIterations: 3` (final stop:true caps actual iterations at 2). Asserts both iter1 and iter2 markers contain "shared" — load-bearing for "applies to every iteration" rule.
  - **T-API-50b (run, intra-workflow goto)**: `ralph:index` writes `index.txt` with MYVAR then gotos `ralph:check`; `ralph:check` writes `check.txt` and emits stop:true. Asserts both markers contain "intra" — load-bearing for "applies across goto destinations" within a single workflow.
  - **T-API-50c (run, cross-workflow goto)**: `alpha:index` writes `alpha.txt` with MYVAR then gotos `beta:step`; `beta:step` writes `beta.txt` and emits stop:true. Asserts both markers contain "cross" — load-bearing for "applies across cross-workflow goto" — a buggy implementation that re-resolved env from a per-workflow context would fail this.
  - **T-API-50d (run, loop reset)**: `ralph:index` increments a counter, writes `run<N>.txt`, returns plain output (no goto, no stop) on iteration 1 — triggering loop reset back to `ralph:index` for iteration 2 (counter = 2 → emits stop:true). Asserts both markers contain "reset" — load-bearing for "applies across loop reset" — a buggy implementation that bound env to the initial spawn rather than the call-time snapshot might fail to propagate after reset.
  - **T-API-50e (runPromise, empty-string value)**: same `${MYVAR+x}` set-vs-empty distinction fixture, driver passes `env: { MYVAR: "" }`. Asserts marker = `present\\t` (set, empty) — load-bearing — a buggy implementation that filtered empty-string values as "unset" would produce `absent`.
  - **T-API-50h (runPromise + run, tricky non-NUL values)**: bash fixture iterates over 11 keys (V_SPACES, V_EQ, V_HASH, V_DQ, V_SQ, V_BS, V_UTF, V_LF, V_CRLF, V_TAB, V_WSONLY) using bash indirect expansion `${!key}` and writes each value to `<key>.txt`. Driver passes a payload covering whitespace (leading/trailing/only), embedded `=`, comment-prefix `#`, double quotes, single quotes, backslashes, UTF-8 + emoji, LF, CRLF, TAB, whitespace-only. Asserts each marker matches the original payload byte-for-byte across BOTH `runPromise()` and `run()` surfaces — load-bearing for "RunOptions.env is NOT subject to env-file parser normalization" — a buggy implementation that ran the values through the §8.1 `parseEnvFile` normalization (stripping leading whitespace, treating `#` as comment, treating `=` as separator) would fail many of these.
- Adjacent suites: zero regressions across env-vars (96), wfdir (28), loop-state (96), execution (57) → 277/277. Full programmatic-api.test.ts run: 384/384 in ~49s.

### ADR-0004 §9.5 / §9.1 / §9.2 (T-API-52/52a/52b/52c/52d/52e/52e2/52f — RunOptions.env snapshot semantics) — RESOLVED

SPEC §9.5 second paragraph (Lifetime): "Entries are captured synchronously at call time as a shallow copy — loopx reads the supplied object's own enumerable string-keyed properties once. The capture runs at the `run()` / `runPromise()` call site …". The contract has multiple structurally-distinct branches that need pinning down independently:
- **Outer-property mutation axis**: post-call value mutation × post-call key-set mutation × both API surfaces. Buggy implementations that held a live reference to the supplied object instead of a snapshot would observably surface post-call mutations to spawned scripts.
- **Per-entry getter axis**: accessor-property values read via `[[Get]]` semantics exactly once at call time, not lazily at first `next()` and not re-invoked per spawn. T-API-52/52a/52b cover plain-object snapshots only — they do not catch a regression where an implementation deferred entry-getter reads to first `next()` (or re-invoked them per spawn) on accessor-defined keys.
- **Proxy trap axis**: `ownKeys` and `get` traps invoked exactly the spec'd number of times at call time. T-API-52c/52d cover the per-entry-getter axis but not the proxy enumeration axis. T-API-62f/62f3 cover the throwing-trap failure path, but a successful proxy must also be enumerated and value-read exactly once.

The implementation in `packages/loop-extender/src/run.ts` (`snapshotEnv` at lines 72-103, called synchronously inside `snapshotOptions` at lines 192-207, called synchronously at the head of `runWithInternal` at line 267) ALREADY conforms: `Object.keys(envRaw)` enumerates own-enumerable string-keyed properties exactly once (which invokes the proxy `ownKeys` trap once and the `getOwnPropertyDescriptor` trap an implementation-defined number of times per SPEC §9.5); `(envRaw)[key]` invokes `[[Get]]` semantics once per included key (proxy `get` trap or accessor getter); the result `out: Record<string, string>` is a freshly-allocated shallow copy detached from the original object. `runPromise` (lines 732-783) calls `runWithInternal` synchronously before its `await Promise.resolve()` microtask boundary, preserving eager-snapshot semantics on the eager surface. Test-only iteration; no implementation changes required.

Now-passing tests (this iteration):
- **programmatic-api.test.ts: 418/418 PASS** (was 398, +20) — added 20 tests (10 IDs × 2 runtimes) under a new `SPEC: RunOptions.env Snapshot Semantics` describe block:
  - **T-API-52 (runPromise — value mutation post-call)**: bash fixture observes MYVAR. Driver: `const e = { MYVAR: "initial" }; const p = runPromise("ralph", { env: e, maxIterations: 1 }); e.MYVAR = "mutated"; await p;` Asserts marker = `"initial"` — load-bearing — a buggy implementation that held a live reference would surface `"mutated"`.
  - **T-API-52a (run — added key post-call)**: bash fixture observes both MYVAR (`${MYVAR+x}` set-vs-empty) and NEWVAR. Driver: `const e = { MYVAR: "initial" }; const gen = run("ralph", { env: e, maxIterations: 1 }); e.NEWVAR = "added"; for await (const o of gen) {}` Asserts MYVAR marker = `"present\tinitial"` and NEWVAR marker = `"absent"` — the shallow snapshot captured initial keys; post-call key addition is not observed.
  - **T-API-52b (run — value mutation between call and first next())**: same single-key fixture. Driver: `const e = { MYVAR: "A" }; const gen = run("ralph", { env: e, maxIterations: 1 }); e.MYVAR = "B"; for await (const o of gen) {}` Asserts marker = `"A"` — proves snapshot is taken at `run()` call site, NOT at first `next()` (would surface `"B"` on lazy-snapshot implementation).
  - **T-API-52c (run — accessor getter call-time eager + no per-spawn re-read)**: `Object.defineProperty(env, "MYVAR", { enumerable: true, configurable: true, get() { count += 1; return backing; } });` Driver captures `count` at three points: after `run()` returns (before any `next()`), after backing-state mutation (`backing = "mutated"`), after `for await` settlement. Asserts countAfterRun=1 (proves eager call-time snapshot), countAfterMutate=1 (frozen — backing-state mutation does not re-invoke), countAfterSettle=1 (no per-spawn re-read), marker = `"initial"`.
  - **T-API-52d (runPromise — accessor getter call-time eager + reused across iterations)**: same accessor; multi-iteration fixture (3 iterations, per-iteration markers). Driver captures `count` at runPromise() call point, after backing mutation, after promise resolution. Asserts countAfterCall=1 (eager call-time), countAfterMutate=1 (frozen), countAfterResolve=1 (no per-spawn re-read across 3 iterations), all three iter markers = `"initial"`.
  - **T-API-52e (run + runPromise — proxy ownKeys+get traps call-time + no per-spawn re-read)**: proxy with custom `ownKeys`, default `getOwnPropertyDescriptor`, custom `get`. Two-key target `{ MYVAR, OTHERVAR }`. 3-iteration fixture. Driver captures trap counts at call site and after settlement. Asserts on each surface: ownKeysAfterCall=1 (call-time enumeration once); ownKeysAfterDone=ownKeysAfterCall (no re-enumeration per spawn); getAfterCall=2 (one [[Get]] per included string key); per-key valueGetSnapshot=1 each; getAfterDone=getAfterCall + per-key counts unchanged (no per-spawn value-re-read); 3 outputs, all 6 markers (3 iter × 2 vars) record snapshot values.
  - **T-API-52e2 (runPromise — descriptor-only proxy)**: only `getOwnPropertyDescriptor` is custom-trapped. SPEC §9.5 leaves descriptor-call cardinality implementation-defined; asserts only descAfterDone === descAfterCall (no per-spawn re-invocation), 3 iter markers all record snapshot values.
  - **T-API-52e2 (runPromise — get-only proxy)**: only `get` is custom-trapped. Asserts getAfterCall=2 (one [[Get]] per included key per SPEC §9.5 normative contract), per-key valueGetSnapshot=1, getAfterDone=getAfterCall (no per-spawn re-read), per-key counts unchanged, 3 iter markers record snapshot values.
  - **T-API-52f (runPromise — added key post-call)**: same two-marker fixture as T-API-52a but on the eager surface. Driver: `const e = { MYVAR: "initial" }; const p = runPromise("ralph", { env: e, maxIterations: 1 }); e.NEWVAR = "added"; await p;` Asserts MYVAR marker = `"present\tinitial"`, NEWVAR marker = `"absent"` — closes the post-call-key-addition axis on the runPromise() surface (T-API-52d/52e cover call-time enumeration cardinality but not post-call additions).
- Adjacent suites: zero regressions across tmpdir (569/569), env-vars + wfdir + loop-state (232/232), signals (9/9), unit + harness (158/158), typecheck OK. Full programmatic-api.test.ts run: 418/418 in ~54s.

### ADR-0004 §9.5 / §8.3 / §13 / §7.4 (T-API-51a/51a2/51b/51c/51d/51e/51f — RunOptions.env precedence chain) — RESOLVED

SPEC §8.3 precedence chain (highest wins): (1) protocol vars `LOOPX_*` (applied at execution time), (2) `RunOptions.env`, (3) local env file (`-e` / `RunOptions.envFile`), (4) global loopx env (`$XDG_CONFIG_HOME/loopx/env`), (5) inherited `process.env`. SPEC §9.5: `RunOptions.env` entries override same-named values from `-e`, the global env file, and inherited `process.env`, and are overridden by protocol variables. SPEC §13: protocol variables silently override user-supplied values (no warning / notice on stderr). The T-API-51 series pins each precedence boundary on the API surface: tier-2 vs tier-1 (51a / 51a2), tier-2 vs tier-3 (51b), tier-2 vs tier-4 (51c), tier-2 vs tier-5 (51d), and the full chain (51e for runPromise, 51f for run).

The implementation in `packages/loop-extender/src/run.ts` (`runInternal` env merging at line 661-664: `{ ...mergeEnv(globalEnv, localEnv, inheritedEnv), ...(snap.env ?? {}) }`) and `packages/loop-extender/src/execution.ts` (protocol-var overlay at line 179-185: `{ ...env, LOOPX_BIN, LOOPX_PROJECT_ROOT, LOOPX_WORKFLOW, LOOPX_WORKFLOW_DIR, LOOPX_TMPDIR }`) ALREADY conformed: `mergeEnv` produces tiers 5→4→3, `snap.env` overlays tier 2, protocol vars overlay tier 1 last per the documented comment block at run.ts:652-660. The silent-override contract holds because no override-warning code exists for any LOOPX_* name. Test-only iteration; no implementation changes required.

Now-passing tests (this iteration):
- **programmatic-api.test.ts: 398/398 PASS** (was 384, +14) — added 14 tests (7 IDs × 2 runtimes) under a new `SPEC: RunOptions.env Precedence` describe block:
  - **T-API-51a (runPromise) / T-API-51a2 (run)**: bash fixture observes all five script-protocol-protected names (`LOOPX_BIN`, `LOOPX_PROJECT_ROOT`, `LOOPX_WORKFLOW`, `LOOPX_WORKFLOW_DIR`, `LOOPX_TMPDIR`) into per-name marker files, AND performs an in-script `[ -d "$LOOPX_TMPDIR" ]` stat (during execution — SPEC §7.4 cleanup runs after the script, so a post-run stat would observe absence) writing `is-dir` / `not-dir` to a stat marker. Driver passes `env: { LOOPX_WORKFLOW: "fake", LOOPX_PROJECT_ROOT: "/tmp/fake", LOOPX_WORKFLOW_DIR: "/tmp/fake-dir", LOOPX_TMPDIR: "/tmp/fake-tmp", LOOPX_BIN: "/tmp/fake-bin" }`. Asserts: (a) each marker records the real protocol value, NOT the user-supplied `/tmp/fake-*` value (load-bearing — a buggy implementation that spread `RunOptions.env` AFTER protocol vars instead of BEFORE would propagate the fake values); LOOPX_BIN marker is a real existing file; LOOPX_PROJECT_ROOT marker equals `realpathSync(project.dir)`; LOOPX_WORKFLOW marker equals `"ralph"`; LOOPX_WORKFLOW_DIR marker equals `join(realpathSync(project.dir), ".loopx", "ralph")`; LOOPX_TMPDIR marker matches `/\/loopx-[^/]+$/` (real loopx-created tmpdir per SPEC §7.4 mkdtemp naming convention `loopx-XXX`). (b) During-run stat marker records `"is-dir"` (load-bearing — proves LOOPX_TMPDIR points to a real loopx-created directory, not merely a substituted string; T-TMP-30 / T-TMP-31 rigor). (c) Stderr matches NO regex `<name>.*(override|overrid|ignored|warning|notice)` for any of the five names (load-bearing — a buggy implementation that emitted a "user-supplied LOOPX_* was overridden" diagnostic to flag the silent-override behavior would fail this even though (a) and (b) would pass).
  - **T-API-51b (runPromise — tier 2 vs tier 3)**: local `.env` at `<project>/.env` contains `MYVAR=from-file`; driver passes `envFile: ".env", env: { MYVAR: "from-options" }`. Asserts marker == `"from-options"` — load-bearing — a buggy implementation that merged `snap.env` BEFORE local-env-file overlay would surface `"from-file"`.
  - **T-API-51c (runPromise — tier 2 vs tier 4)**: temp `XDG_CONFIG_HOME` directory with `<xdg>/loopx/env` containing `MYVAR=from-global`; driver sets `process.env.XDG_CONFIG_HOME = <xdg>` then calls `runPromise("ralph", { env: { MYVAR: "from-options" } })`. Asserts marker == `"from-options"`. try/finally cleans up the temp xdg dir.
  - **T-API-51d (runPromise — tier 2 vs tier 5)**: driver sets `process.env.MYVAR = "inherited"` then calls `runPromise` with `env: { MYVAR: "from-options" }`. Asserts marker == `"from-options"`.
  - **T-API-51e (runPromise — full chain)**: configures all four lower tiers — `process.env.MYVAR = "1"` (tier 5), `process.env.XDG_CONFIG_HOME = <xdg>` with `MYVAR=2` (tier 4), local `.env` with `MYVAR=3` (tier 3), `RunOptions.env: { MYVAR: "4" }` (tier 2). MYVAR is not a protocol name so tier 1 doesn't apply. Asserts marker == `"4"` — load-bearing — any inverted overlay order would surface a different lower-tier value. try/finally cleans up the temp xdg dir.
  - **T-API-51f (run — full chain)**: same fixture as T-API-51e, driver consumes via `for await`. Verifies the run() generator surface routes the same merged env to every executeScript invocation (a buggy implementation that wired correct precedence on `runPromise` but routed `run()` through a different env-resolution path would fail this counterpart).
- Adjacent suites: zero regressions — env-vars + wfdir + loop-state + execution (277/277), tmpdir.test.ts (569/569), signals (9/9), unit + harness (158/158), typecheck OK. Full programmatic-api.test.ts run: 398/398 in ~51s.

## P1 — REMAINING T-TMP-* subsections

These T-TMP IDs are not yet implemented as test cases or are blocked by missing infrastructure:

- All previously-listed T-TMP-* RESOLVED — see the RESOLVED subsections above for the full list (T-TMP-34/34a/34b symlink rule-2; T-TMP-35/35a/35b rule-3; T-TMP-35c/35d/35e cleanup-warning × script-error; T-TMP-35f/35g/35h cleanup-warning × signal/abort; T-TMP-36/36a/36b rule-5; T-TMP-37/37a/37b/37c/37d/37e rule-3 generic-dispatch + rule-4 nested-symlink walk; T-TMP-38/38f/39 racing SIGINT/SIGTERM idempotence; T-TMP-38a/38a2/38b/38b-run/38b2/38b2-run/38c/38c2 cleanup-start/abort-listener/consumer-throw-observed seam matrix; T-TMP-38d/T-TMP-38d2/T-TMP-38d3/T-TMP-38d4 post-final-yield consumer-cancellation × abort 2×2 matrix; T-TMP-38e/T-TMP-38e-run spawn-failure × abort cleanup idempotence on programmatic surfaces; T-TMP-40/40a/40b lstat-fail seam; T-TMP-41/41a/41b symlink-unlink-fail seam; T-TMP-42/42a/42b/42c/42d/42e cleanup-fault × every terminal type).

## P1 — Discovered open issues

(none currently open)

## P1 — Other ADR-0004 test suites still missing

- **T-API-50..59i** — `RunOptions.env` block (~80 IDs); the inherited-env snapshot-timing subset (T-API-71/71a/71b/72/72a/72b — lazy under `run()`, eager under `runPromise()`), the basic-injection subset (T-API-50/50a/50b/50c/50d/50e/50h — RunOptions.env reaches every spawned script across iterations / intra-workflow goto / cross-workflow goto / loop reset / empty-string and tricky-non-NUL value preservation), the precedence chain (T-API-51a/51a2/51b/51c/51d/51e/51f — tier-2 vs tier-1 silent-override × all five LOOPX_* names + tier-2 vs tier-3/4/5 + full chain on both surfaces), and the snapshot-semantics matrix (T-API-52/52a/52b/52c/52d/52e/52e2/52f — value-mutation × key-addition × accessor-getter × proxy ownKeys/get/descriptor-only trap-call cardinality on both surfaces) are now in place. Remaining `RunOptions.env` invalid-shape (T-API-53/54/55), filtering (T-API-56), runtime-rejected names/values (T-API-57/58), lookup-isolation (T-API-59), tmpdir-parent isolation (T-API-50f/50g concurrent + T-API-60), and abort × env-snapshot interactions still need authoring.
- **T-API-60..62i4** — Pre-iteration ordering and options-snapshot tests (~60 tests).
- **T-API-63..69u** — Abort precedence, generator lifecycle, promise rejection (~150 tests). T-API-66/66a/66b/66c/66d/66e (the API-surface OUTCOME-axis counterparts to T-TMP-23/24a/24c/24d/24e/24f/24g — abort-after-final-yield × `.next()`/`.return()`/`.throw()` × maxIterations / stop:true matrix) are now RESOLVED — see RESOLVED subsection above. Remaining T-API-63..65, T-API-67..69u still need authoring.
- **T-API-70..74c** — 15 remaining programmatic API tests.
- **T-INST-110..120e** — Auto-install ADR-0004 (~100 tests): `npm install`, `--no-install`, `.gitignore` safeguard, malformed `package.json` skip, npm-install failure handling, signals during npm install, no-rollback semantics, `-y` interaction, environment isolation, streaming passthrough, npm-only manager selection.
- **T-INST-55f..55zj** — symlink source validation (~40 tests).
- **T-INST-40f, T-INST-44a..44f** — `--no-install` CLI parsing (duplicate rejection, help interaction, no short alias).
- **T-INST-DASHDASH-01..04** — `--` rejection on install.
- **T-INST-42m, 42n** — install help-text content pins.

## P2 — Supplementary tests for established sections

- **Loop state**: T-LOOP-13a, 15b, 24a, 44, 45, 46.
- **Discovery**: T-DISC-07a, 07c, 09a, 09b, 24a, 24b, 42d-42g, 49a-49k, 40j-40w (project-root `.loopx` entry failures and symlink edges).
- **Output parsing**: T-PARSE-04a, 13a, 17a; T-MOD-13q.
- **Execution edge cases**: T-EXEC-03b, 03c, 07a, 07b, 13c-13m (CJS/`require` rejection), 15a/15b/15c (no auto-install at run time), 16c, 16d.
- **Signals**: T-SIG-04a, 05a, 06a, 07a (SIGINT parity); T-SIG-20..31 (full pre-iteration signal-wins precedence).
- **Types**: T-TYPE-08 (compile-time check for `output()`/`input()`).
- **Env**: T-ENV-05f, 08a, 15g..15n; T-ENV-21e..21h; T-ENV-24a2..24a6; T-ENV-26..26g (NUL bytes); T-ENV-27..27e (`RunOptions.env` tier interaction); T-ENV-28..29a.
- **Exit codes**: T-EXIT-17 (invalid target string).
- **Source detection**: T-INST-05a, 08g..08m.
- **Other install**: T-INST-56f..56i, 60t/60u, 63f/63g, 64e, 70e, 76b, 79a, 80c2, 80f2, 83b, 92a..92c, 97a2, 97c.

## Pre-existing failures (not ADR-0004)

- **T-INST-GLOBAL-01a** — `[Bun] full global install lifecycle with import 'loopx'` fails with "Module not found … /loop-extender/bin.js". Pre-existing issue with Bun's resolution at the global-install layout. Unrelated to ADR-0004; confirmed by reproducing the failure on baseline (pre-change) source. Should be triaged separately.
- **Bun-under-full-parallelism failures** — Running the entire `npm run test` matrix in full parallelism surfaces ~32 additional `[Bun]`-runtime failures across module-resolution / wfdir / execution / install test files. Confirmed pre-existing via `git stash`: the same 32 failures reproduce with this iteration's changes stashed, and they do NOT reproduce when running the e2e suites alone (e2e-only run shows 1 failure: T-INST-GLOBAL-01a above). Likely a parallelism / isolation issue in the test harness or Bun's loader behavior under concurrent test workers; unrelated to ADR-0004 and out of scope for this work.

## Notes

- `.loopx/.iteration.tmp` and `.loopx/ralph/.tmp/` appear in `git status` — left by prior loopx runs, not part of this work. Not tracked.

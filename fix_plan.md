# Implementation Plan for loopx Test Harness

**Status: Test Harness Significantly Out of Sync with TEST-SPEC.md (ADR-0003)**

## Audit Results (2026-04-17)

Prior "Production Ready — All 1068 tests pass" claim was **incorrect**.

- TEST-SPEC.md defines **970 unique test IDs** (post-ADR-0003).
- tests/ directory implements **534 unique test IDs** → **~436 IDs missing** (audit baseline; stale now that the P0 rewrites — cli-basics, discovery, delegation, install, programmatic-api, loop-state, execution, env-vars, subcommands, module-resolution, plus the new version-check file — are landed).
- **0 test files reference "workflow"** — tests still encode the pre-ADR-0003 flat-script / directory-script model (also stale post-rewrites).
- tests/helpers/fixtures.ts lacks the TEST-SPEC §2.3 workflow helpers (`createWorkflow`, `createWorkflowScript`, `createBashWorkflowScript`, `createWorkflowPackageJson`).
- src/ still uses the pre-ADR-0003 model (`src/discovery.ts` scans flat files and directory scripts; `src/validate-dir-script.ts` remains).
- The 1068 currently-passing tests pass because **tests and implementation are consistently pre-ADR-0003**, but TEST-SPEC has moved on.

Per PROMPT.md: goal is to implement the test harness **first**; some tests are expected to fail until the implementation catches up. We are building the test harness to the workflow model per TEST-SPEC.md.

## Priority Ordering

### ✅ P0 — Workflow fixture helpers (landed, commit 7cbe556)

All four workflow helpers in `tests/helpers/fixtures.ts` are implemented and exported via `tests/helpers/index.ts`: `createWorkflow`, `createWorkflowScript`, `createBashWorkflowScript`, `createWorkflowPackageJson`. Matches TEST-SPEC §2.3 lines 134-159.

### P0 — Test suite rewrite (workflow-model alignment)

Every implemented test currently uses the flat-script model. Samples confirmed: `T-CLI-01` creates `.loopx/myscript.sh`; `T-DISC-01`/`T-LOOP-01` follow the same pattern. TEST-SPEC has these under the workflow model (`.loopx/myscript/index.sh` for bare targets, `workflow:script` for nested). The tests must be rewritten rather than augmented.

### ✅ P0 — Completed rewrites

- [x] **Rewrote tests/e2e/delegation.test.ts** — covers all 29 T-DEL-* IDs documented in TEST-SPEC §4.12 (post-ADR-0003 §5: project-root-only delegation, ancestor traversal removed, project-root `package.json` failure modes, dependency-field precedence, delegation × version-check interaction). Positive delegation (T-DEL-01, 05, 06, 07, 08, 17, 18, 19); recursion guard (T-DEL-04, 09); signals preserved across delegation (T-DEL-10, 11); ancestor-traversal removed (T-DEL-02); CWD-only delegation works before `.loopx/` exists (T-DEL-03); project-root `package.json` failure modes — no file / unreadable / invalid JSON / declared-but-missing-binary / binary-without-declaration (T-DEL-12, 13, 14, 14a, 14b, 15, 16); workflow-level `package.json` does NOT trigger delegation (T-DEL-20); delegation applies to all commands — `install -h` / top-level `-h` / bare `loopx` / `env list` (T-DEL-21, 22, 23, 26); runtime + install-time delegation × workflow-level version check (T-DEL-24, 25) using a functional local loopx that symlinks real files but rewrites `package.json` to a target version; `peerDependencies` excluded from delegation-checked fields (T-DEL-27). Uses `withDelegationSetup` for baseline fixtures and inline `mkdtemp` setups for tests that need custom project-root or parent/child layouts (T-DEL-02, 03, 06, 20, 24, 25). Warning-shape assertions use prose-tolerant regex predicates (`hasUnreadableDelegationWarning`, `hasInvalidJsonDelegationWarning`, `hasMissingBinaryWarning`) over stderr — tolerant of specific wording but pinning down failure-mode semantics. T-DEL-13 uses `it.skipIf(IS_ROOT)` since chmod-000 is a no-op under root. T-DEL-24 / T-DEL-25 introduce a new helper `installFunctionalLocalLoopx(projectDir, version)` that symlinks every file from the real `node_modules/loopx/` except `package.json`, which is rewritten with a target `version` field so workflow-level version checks see the delegated-to local binary's version (not the global launcher's). `.bin/loopx` is a bash wrapper that execs the local `bin.js`. Current pass/fail against the still-pre-ADR-0003 src/ implementation: 15 pass, 14 fail (29 total). Passing tests (T-DEL-01, 03, 04, 05, 06, 07, 08, 09, 17, 18, 19, 21, 22, 23, 26) are those whose expected outcome incidentally aligns with pre-ADR-0003 ancestor-traversal delegation. Failing tests (T-DEL-02, 10, 11, 12, 13, 14, 14a, 14b, 15, 16, 20, 24, 25, 27) are those that require ADR-0003 §5 behavior — removed ancestor traversal, declaration-based delegation, project-root `package.json` failure-mode warnings, workflow-dir-as-cwd execution. T-DEL-10 and T-DEL-11 fail because `.loopx/sleeper/index.sh` is a workflow per ADR-0003, not a flat/directory script, so pre-ADR-0003 loopx errors out before the sleep script can emit `ready` on stderr. Tests compile cleanly (`npx tsc --noEmit` passes; vitest reports "Type Errors: no errors"). Runtime ~1.4s total for 29 tests.

- [x] **Rewrote tests/e2e/install.test.ts** — covers all 186 T-INST-* IDs documented in TEST-SPEC §4.10 (post-ADR-0003 workflow model): Source Detection (T-INST-01, 01a, 02–08, 08a, 08c–08f), Install CLI Parsing (T-INST-40–49e), Late-Help Short-Circuit (T-INST-49a–49g), Workflow Classification — Single-Workflow Source (T-INST-50, 50a, 50b ×5, 51, 52, 52a, 52c, 52d), Workflow Classification — Multi-Workflow Source (T-INST-53, 53a, 53b, 53c ×5, 54, 54a, 54b, 54c, 54d, 55, 55a, 55b, 55c, 55d, 55e), Workflow Classification — Zero-Workflow Source (T-INST-56, 56a, 56b, 56c, 56d, 56e), Selective Workflow Installation (T-INST-57, 57a, 57b, 57c, 58, 59, 59a, 59b, 60, 60a–60s), Install-time Validation (T-INST-52b, 61, 62, 62a, 63, 63a–63e, 64, 64a, 64b, 64c, 64d), Collision Handling (T-INST-65, 66, 67, 67a, 68, 69, 70, 70a, 70b, 70c, 70d, 71, 71a, 97), Version Checking on Install (T-INST-72, 72a, 73, 73a, 74, 74a, 75, 75a, 76, 76a), Install Atomicity (T-INST-77, 78, 79, 80, 80a, 80b, 80c, 80d, 80e, 80f, 80g, 80h, 80i, 80j), Tarball Install (T-INST-81, 82, 83, 83a, 84, 85, 85a, 85b, 86, 86a), Git Install (T-INST-87, 88, 89), Common Rules (T-INST-90, 91, 92, 93, 94, 95, 96, 97a, 97b), Global Install Smoke Test (T-INST-GLOBAL-01, 01a). 386 cases total (two parameterized runtime sweeps × 193 IDs, since T-INST-50b and T-INST-53c are each 5-extension parameterized). Uses only workflow helpers (`createTempProject`, `createWorkflow`, `createWorkflowScript`, `createBashWorkflowScript`, `createWorkflowPackageJson`) from tests/helpers/fixtures.ts; does NOT reference the legacy `createScript`/`createDirScript`/`createBashScript` (legacy helpers kept pending the P1 cleanup commit). Tarball fixtures use a new `makeTarball` helper that builds archives via `python3`'s `tarfile` module so per-entry modes (including 0o000 for unreadable-`package.json` fixtures) can be carried without requiring non-root `tar` to read mode-000 source files; this was the blocker for T-INST-60r/74/74a/79/80b/80d. The helper supports `wrapperDir` (single top-level wrapper stripped on extract), flat top-level layout (multi-top, no stripping), `corrupt` (truncated gzip stream for T-INST-94), and `empty` (tar with no entries for T-INST-95). Warning-shape assertions use `hasVersionMismatchWarning` / `hasInvalidJsonWarning` / `hasInvalidSemverWarning` / `hasUnreadableWarning` / `hasAnyPackageJsonWarning` / `hasWarningCategoryFor` predicates modeled on version-check.test.ts (tolerant of specific prose while pinning down workflow attribution and per-workflow dedupe). Unreadable-file tests (T-INST-60r, 74, 74a, 79, 80b, 80d) use `it.skipIf(IS_ROOT)` where `IS_ROOT = process.getuid?.() === 0` because Git does not reliably preserve mode 000 across clone (per SPEC) and non-root permission semantics are required to exercise the failure path. T-INST-80c uses the `LOOPX_TEST_INSTALL_FAULT=commit-fail-after:1` env seam (with `NODE_ENV=test`) per TEST-SPEC §1.4. T-INST-42a/b/d/g/h/k assert that install-help short-circuit fires before network activity by inspecting `readdirSync(project.loopxDir).length === 0` — the HTTP server will still respond but no install-side state is produced. Current pass/fail against the still-pre-ADR-0003 src/ implementation: 118 pass, 268 fail (386 total = 193 × two runtimes). Passing tests are those whose expected outcome (usage errors on invalid grammar, collision refusals, zero-workflow errors, invalid-name errors) incidentally aligns with pre-ADR-0003 behavior; failing tests are those that require ADR-0003 workflow-model classification, selective install (`-w`), preflight atomicity, workflow-level version checking, `-y` override semantics, and the new error messages for config-style root files. Tests compile cleanly (`npx tsc --noEmit` passes).

- [x] **Rewrote tests/e2e/programmatic-api.test.ts** — covers all 173 T-API-* IDs documented in TEST-SPEC §4.9 (`run()` AsyncGenerator, `run()` with Invalid target, `run()` Target Semantics, `run()` Snapshot & Cancellation, `run()` with AbortSignal, `runPromise()`, `runPromise()` Target Semantics, `runPromise()` with Invalid target, `runPromise()` Snapshot & Options, Error Behavior, envFile Option, maxIterations Validation, `runPromise()` with AbortSignal). 346 cases total (173 × node/bun). Uses only the workflow helpers (`createWorkflow`, `createWorkflowScript`, `createBashWorkflowScript`, `createWorkflowPackageJson`) from tests/helpers/fixtures.ts; does NOT reference the legacy `createScript`/`createDirScript`/`createBashScript` (legacy helpers kept pending the P1 cleanup commit). Targets use the ADR-0003 `workflow:script` shape exclusively: `run("ralph")` for `ralph:index`, `run("ralph:check")` for qualified targets, `runPromise("workflow:script")`, etc. `RunOptions.cwd` is treated as the **project root** (for `.loopx/` resolution and `LOOPX_PROJECT_ROOT`), not the script execution cwd — verified by T-API-07a and T-API-47b which assert the script's actual `process.cwd()` is the workflow directory (`.loopx/ralph/`), not the provided RunOptions.cwd. Version-warning tests use tolerant name-scoped regex predicates mirroring version-check.test.ts (`hasVersionMismatchWarning`, `countVersionMismatchWarnings`, `hasInvalidJsonWarning`, `hasUnreadableWarning`, `hasInvalidSemverWarning`, `hasAnyPackageJsonWarning`) so they pin down the shape and workflow attribution without locking in specific warning wording. Unreadable-file tests (T-API-08l, 08o, 08p, 08p2, 08t3, 08t7, 08u2, 08y, 08z4, 14b3, 14j3, 14j7, 14k2, 14o, 14p4) use `it.skipIf(IS_ROOT)` where `IS_ROOT = process.getuid?.() === 0`. Post-discovery ordering tests (T-API-35c, 35f, 44b, 44c) create `.loopx/broken/check.sh` + `.loopx/broken/check.ts` name-collision sibling alongside `.loopx/valid/index.sh` and assert the thrown error message contains `"broken"` — proving discovery/global validation runs before the invalid target format is rejected (per SPEC 4.1/9.1/9.2 "invalid targets are rejected at the same point as a missing workflow"). The cross-workflow goto → loop-reset fixtures (T-API-08ab, T-API-14r) use counter files to alternate `alpha → beta → alpha → beta → stop` so that both workflows are entered exactly twice and the "first-entry-only" dedupe rule can be verified by counting warnings. Current pass/fail against the still-pre-ADR-0003 src/ implementation: 122 pass, 224 fail (346 total). The passing tests are those whose expectations (errors/rejections for missing workflows / invalid targets / name collisions) incidentally align with the pre-ADR-0003 "script not discovered" error path — e.g., all the T-API-14a*/38/39/40/41/42/43/44/44a/45/45a/46/20* rejection tests. Failing tests are those that require ADR-0003 workflow discovery and version-check machinery to fire correctly. Tests compile cleanly (vitest reports "Type Errors: no errors"); `npx tsc --noEmit` passes. Runtime ~132s.
- [x] **Rewrote tests/e2e/cli-basics.test.ts** — covers every T-CLI-* ID documented in TEST-SPEC §4.1 (154 unique IDs, 308 cases at 2 runtimes × 154). Grouped by subsection: Help & Version (T-CLI-01); Top-Level Help (02, 03, 04, 05, 06, 07e, 07f, 07g, 07j, 39, 61, 90, 91); Run Help (40, 40a, 41, 42, 43, 43a, 44, 101, 101a, 102, 104, 104a–104e, 105, 106, 62, 120, 120a, 120b, 121, 122); Run Help Short-Circuit (48, 49, 50, 51, 52, 53, 54, 63, 67, 68, 69, 70, 92, 93, 94, 95); Late-Help Short-Circuit (73, 74, 75, 76, 77, 78, 78a, 78b, 78c, 78d, 84); Bare Invocation & Top-Level Parsing Errors (28, 33, 34, 71, 36, 37, 07b, 07c, 38, 79); Target Invocation via run (30, 11, 107, 108, 109, 109a, 110, 12, 111, 111a, 13, 29, 64, 65, 59, 60, 85, 31, 32, 66, 80, 81); Target Validation (112, 113, 114, 115, 116, 117, 118, 118a, 114a, 118b); Option Order (57, 58, 83); CLI -n Option (14, 15, 16, 17, 18, 19, 19a, 20, 56, 119, 119c, 119e, 119f, 119a, 119b, 119d, 119g, 119h, 119i, 119j, 119k); Duplicate Flags (20a, 20b); Unrecognized Run Flags (35, 72, 86, 87, 88, 89); Missing Flag Operands (97, 98, 99, 100); CLI -e Option (21, 22, 22a, 22c, 22b, 22d, 22e, 22f); CLI Stdout Silence + Extra Positionals (23, 27, 96). Uses only workflow helpers (`createWorkflow`, `createWorkflowScript`, `createBashWorkflowScript`, `createWorkflowPackageJson`) from tests/helpers/fixtures.ts; does NOT reference `createScript`/`createDirScript`/`createBashScript` (legacy helpers kept pending the P1 cleanup commit). Symlink tests (T-CLI-121, 122) use `symlinkSync` + `mkdtemp` under `tmpdir()` with extra cleanup tracking. Unreadable-file tests (T-CLI-119e/119k/22f) use `it.skipIf(process.getuid?.() === 0)`. Pre-ADR-0003 directory-script tests (T-CLI-45, 46, 47, 55, 55a–55d, 82, 103) are removed as they are orphans under ADR-0003 §4.1. Current pass/fail against the still-pre-ADR-0003 src/ implementation: 202 pass, 106 fail (308 total = 154 × node/bun). Passing tests are those whose expected outcome (e.g., exit 1 on usage errors, top-level help display, `loopx version`) incidentally aligns with pre-ADR-0003 behavior. Failing tests are the ones that require ADR-0003 discovery to list workflows/scripts in run help, emit target-shape error messages, or detect workflow-local collisions. Once src/ migrates to ADR-0003, the 106 should pass for the right reasons. Tests compile cleanly (vitest "Type Errors: no errors"); `npx tsc --noEmit` passes. Runtime ~7.6s.
- [x] **Rewrote tests/e2e/discovery.test.ts** — covers every T-DISC-* ID documented in TEST-SPEC §4.3, grouped by subsection (Workflow Discovery, Script Discovery Within Workflows, Default Entry Point, Name Collisions Within Workflows, Workflow and Script Naming, Previously Reserved Names, Symlinks, Discovery Caching, Validation Scope, Discovery Scope). Concretely: T-DISC-01–11, 10a–10g, 12, 13, 14, 14a, 14b, 15, 15a, 15b, 16, 17, 18, 19, 20, 20a, 20b, 20c, 21, 21a, 22, 23, 24, 25, 26, 26a, 26b, 27, 28, 29, 30, 30a, 30b, 31, 32, 33, 34, 35, 36, 37, 38, 39, 39a, 40, 40a–40i, 41, 42, 42a, 42b, 42c, 43, 44, 45, 46, 47, 47a, 47b, 48, 48a — 83 cases total. Uses only the workflow helpers (`createWorkflow`, `createWorkflowScript`, `createBashWorkflowScript`, `createWorkflowPackageJson`) from tests/helpers/fixtures.ts; legacy `createScript`/`createDirScript`/`createBashScript` are not referenced and remain for other tests per the P1 cleanup phase. Current pass/fail against the still-pre-ADR-0003 src/ implementation: 25 pass, 58 fail — matches PROMPT.md's expectation that tests ahead of implementation should fail. Full suite runs in ~4s (previously ~94s on first draft; trimmed 90s of timeouts by making fixture scripts in T-DISC-10a/20b/20c exit 0 immediately so pre-ADR-0003 dir-script interpretation does not hang). Structural "no migration-warning category" check in T-DISC-10a and T-DISC-20c uses a helper `hasWarningCategoryFor(stderr, subject)` that flags any line starting with warning/notice/advisory/migration/deprecation that names the given subject path — no prose-level phrase blacklisting. T-DISC-48a uses `runAPIDriver("node", ...)` from tests/helpers/api-driver.ts to exercise the programmatic `runPromise` + `{cwd}` API. T-DISC-47 uses `startLocalGitServer` from tests/helpers/servers.ts for the install source, since ADR-0003 removed single-file URL install.
- [x] **Created tests/e2e/version-check.test.ts** — covers all 66 T-VER-* IDs. Runs 132 cases (66 × two runtimes), ~126 fail against the pre-ADR-0003 implementation as expected per PROMPT.md; the remaining ~6 pass incidentally (`run -h` path already skips version checking; `loopx install` already rejects unrecognized workflow layouts). Assertion style: name-scoped regex matchers over stderr lines — tolerant of specific warning wording while pinning down shape, workflow attribution, and per-workflow dedupe counts.
- [x] **Rewrote tests/e2e/loop-state.test.ts** — covers all 48 T-LOOP-* IDs from TEST-SPEC §4.6, grouped by subsection: T-LOOP-01–05 (basic loop behavior), T-LOOP-06–10 (`-n` counting), T-LOOP-11–15 plus T-LOOP-15a (input piping, including the new cross-workflow stdin piping case), T-LOOP-16–19 plus T-LOOP-18a/19a/19b (intra-workflow goto semantics with the new ADR-0003 bare-goto-vs-workflow-name disambiguation tests), T-LOOP-30–43 plus T-LOOP-30a/31a/31b/31c/32a (cross-workflow goto semantics — the entire post-ADR-0003 section), T-LOOP-20–24 (error handling), and T-LOOP-25 (final iteration output). Uses workflow helpers exclusively (`createWorkflowScript`, `createBashWorkflowScript`); does NOT reference `createScript` or `createDirScript` (legacy helpers kept pending the P1 cleanup commit). Starting targets use `workflow:script` form — `ralph` (meaning `ralph:index`), `alpha`, and `ralph:check` for the explicit script start in T-LOOP-43. Current pass/fail against the still-pre-ADR-0003 src/ implementation: 32 pass, 64 fail (96 total = 48 × node/bun). The passing tests are those whose expectations (exit 1 on script-not-found) incidentally align with the pre-ADR-0003 "flat-script not discovered" error path — e.g., T-LOOP-18a/19/19a/20/21/34/35/36/37/38/39/40/41/42. Once src/ migrates to ADR-0003, the remaining tests should pass for the right reasons. Tests compile cleanly (vitest reports "Type Errors: no errors"); `npx tsc --noEmit` passes.
- [x] **Rewrote tests/e2e/execution.test.ts** — covers every T-EXEC-* ID documented in TEST-SPEC §4.4, grouped by subsection: T-EXEC-01, 02, 03, 03a, 04, 04a, 04b, 04c (Working Directory / Environment — workflow-directory cwd semantics, `LOOPX_WORKFLOW` env var injection, cross-workflow cwd switching); T-EXEC-05, 06, 07 (Bash Scripts — shebang, `chmod +x`, exit-code propagation); T-EXEC-08, 09, 10, 11, 12, 13, 13a, 13b, 14 (JS/TS Scripts — ESM imports, workflow-local `package.json`, Node vs Bun runtime selection, TS-only-in-Bun); T-EXEC-15, 16, 16a, 16b (Workflow-Local Dependencies — `node_modules` resolution, missing-dep failures, cross-workflow dep isolation). Uses only the workflow helpers from tests/helpers/fixtures.ts (`createWorkflow`, `createWorkflowScript`, `createBashWorkflowScript`, `createWorkflowPackageJson`); does NOT reference the legacy `createScript` / `createDirScript` / `createBashScript` (legacy helpers kept pending the P1 cleanup commit). The obsolete pre-ADR-0003 T-EXEC-17, T-EXEC-18, T-EXEC-18a directory-script tests are removed as they are orphans in TEST-SPEC §4.4. Runtime parameterization: tests without a runtime marker in TEST-SPEC use `forEachRuntime` (runs once per available runtime — Node and Bun); T-EXEC-13 is explicitly `[Node]` (outside `forEachRuntime`); T-EXEC-13b and T-EXEC-14 are explicitly `[Bun]` (guarded with `it.skipIf(!isRuntimeAvailable("bun"))`). Cross-workflow fixtures (T-EXEC-03a, 04b, 04c, 16b): the target script emits `{"stop":true}` after writing its marker so the chain ends cleanly; T-EXEC-04c uses a bare no-goto `beta:step` to trigger loop reset and verifies `alpha-marker` contains `"alphaalpha"` (alpha runs on iterations 1 and 3) and `beta-marker` contains `"beta"` with `-n 3`. T-EXEC-15 creates a workflow-local `node_modules/my-local-lib/` manually (via `mkdir`+`writeFile` under `.loopx/with-deps/`) since the helpers only create one file each — consistent with how other workflow-model tests install deps. Current pass/fail against the still-pre-ADR-0003 src/ implementation: 4 pass, 41 fail (45 total). The 4 passing tests are T-EXEC-13a (CJS `require()` fails) and T-EXEC-16a (missing-dep fails) across Node + Bun — both assert exit != 0, which happens to match the pre-ADR-0003 "workflow not found" error path. Once src/ migrates to ADR-0003, the remaining 41 should pass for the right reasons. Tests compile cleanly (vitest reports "Type Errors: no errors"); `npx tsc --noEmit` passes.
- [x] **Rewrote tests/e2e/env-vars.test.ts** — covers all 48 T-ENV-* IDs documented in TEST-SPEC §4.7 (Global Env File, Env File Parsing, Local Env Override, Injection Precedence, Env Caching). Grouped by subsection: T-ENV-01/02/03/04/05 plus T-ENV-05a–05e (Global Env File); T-ENV-06 through T-ENV-15 plus T-ENV-15a–15f (Env File Parsing via `parseEnvAndObserve` helper); T-ENV-16/17/17a/18/19 (Local Env Override `-e`); T-ENV-20/20a/20b/21/21a/21b/21c/21d/22/23/24/24a/24b (Injection Precedence); T-ENV-25/25a/25b/25c (Env Caching). 96 cases total (48 × node/bun via `forEachRuntime`). Added the six missing ADR-0003 tests: T-ENV-20b (LOOPX_BIN overrides local `-e`), T-ENV-21b (LOOPX_WORKFLOW always set, overrides global env), T-ENV-21c (LOOPX_WORKFLOW overrides local `-e`), T-ENV-21d (LOOPX_PROJECT_ROOT overrides local `-e`), T-ENV-25b (unreadable global env with `-n 0` → exit 1, env loads before short-circuit), T-ENV-25c (malformed-but-readable global env with `-n 0` → exit 0 with parser warning). Fixed T-ENV-24a semantics: previously asserted `LOOPX_DELEGATED` was stripped from script env (`present: false`); TEST-SPEC §4.7 now requires loopx to pass it through when inherited (`present: true, value: "1"`) since the spec does not require scrubbing. Uses only the workflow helpers (`createWorkflowScript`, `createBashWorkflowScript`) from tests/helpers/fixtures.ts — does NOT reference the legacy `createScript`/`createBashScript` (legacy helpers kept pending the P1 cleanup commit). All scripts now live at `.loopx/ralph/index.<ext>` and are invoked via `loopx run -n <N> ralph`. Unreadable-file tests (T-ENV-05a/05b/05c/05d/05e/17a/25b) use `it.skipIf(IS_ROOT)` where `IS_ROOT = process.getuid?.() === 0`. Current pass/fail against the still-pre-ADR-0003 src/ implementation: 18 pass, 78 fail (96 total). Passing tests are those whose expectations (env subcommand errors, unreadable-file exit paths, `-e` nonexistent/unreadable rejection) incidentally align with pre-ADR-0003 behavior — specifically T-ENV-05/05a/05b/05c/05d/05e, T-ENV-17/17a, T-ENV-25b. Failing tests require ADR-0003 workflow discovery of `.loopx/ralph/index.sh`, LOOPX_WORKFLOW env injection, LOOPX_PROJECT_ROOT injection, and env-file parsing warnings. Tests compile cleanly (`npx tsc --noEmit` passes; vitest reports "Type Errors: no errors"). Runtime ~3.4s total for 96 tests.

- [x] **Rewrote tests/e2e/subcommands.test.ts** — covers all 43 T-SUB-* IDs documented in TEST-SPEC §4.2, grouped by subcommand: `loopx output` (T-SUB-01, 02, 02a, 02b, 02c, 02d, 02e, 02f, 02g, 02h, 02i, 02j, 02k, 03, 04, 05, 06, 06a, 06b); `loopx env set` (T-SUB-07, 08, 09, 10, 11, 12, 13, 14, 14a, 14b, 14c, 14d, 14e, 14f, 14g); `loopx env set` on-disk serialization (T-SUB-14h, 14i, 14j, 14k); `loopx env remove` (T-SUB-15, 16); `loopx env list` (T-SUB-17, 18, 19). 86 cases total (43 × two runtimes via `forEachRuntime`). Adds the 11 new T-SUB-02a–02k tests introduced by ADR-0003 for `loopx output --goto` qualified-target serialization (workflow:script form, invalid-shape matrix, name-pattern violations in both portions of qualified targets, syntactically valid but nonexistent targets). Previously had 32 tests; 11 new ADR-0003 tests added. Uses only `createTempProject` and `runCLI` (plus `withIsolatedHome` for global config isolation) — `loopx output`/`env` subcommands do not need workflow fixtures; `loopx output` is a pure JSON serializer and `loopx env` operates on `$XDG_CONFIG_HOME/loopx/env` (the global config file), both independent of the workflow model. Removed the unused `withGlobalEnv` import (dead import left over from earlier refactor). Added `forEachRuntime` parameterization so every T-SUB-* ID runs under both Node and Bun per TEST-SPEC §1.1. All 86 tests currently pass against the pre-ADR-0003 src/ implementation because `loopx output` and `loopx env` subcommands are unaffected by the workflow-model migration (output subcommand is a pure serializer with no validation; env subcommands operate on the global config, not `.loopx/`). The "companions" referenced in the original fix_plan task line — T-CLI-66, T-CLI-80, T-CLI-81, T-MOD-21a — live in cli-basics.test.ts (already rewritten) and module-resolution.test.ts (pending P0 rewrite), not subcommands.test.ts. Tests compile cleanly (`npx tsc --noEmit` passes). Runtime ~3s.

- [x] **Rewrote tests/e2e/module-resolution.test.ts** — covers all 44 unique T-MOD-* IDs documented in TEST-SPEC §4.8 (workflow-local `node_modules/loopx` precedence, delegation-vs-module-resolution interaction, ESM/CJS import semantics, package.json `type` field honoring, `output()` goto-shape non-validation at serialization time). 80 cases total (4 delegation tests + 1 Node-only import + 1 Bun-only import + 1 Node-only T-MOD-03c + 1 Node-only T-MOD-22 + runtime-parameterized tests via `forEachRuntime` across Node + Bun). Adds the new ADR-0003 IDs: T-MOD-03b, 03c, 05a, 13h–13p, 21a. Uses only workflow helpers (`createWorkflow`, `createWorkflowScript`, `createBashWorkflowScript`, `createWorkflowPackageJson`) from tests/helpers/fixtures.ts; does NOT reference the legacy `createScript`/`createDirScript`/`createBashScript` (legacy helpers kept pending the P1 cleanup commit). T-MOD-19, 20, 21, 21a use `withDelegationSetup` (not `runCLI`) per spec note so `$LOOPX_BIN` resolves to a real executable on PATH. T-MOD-03a creates workflow-local `node_modules/loopx` at `.loopx/ralph/node_modules/loopx/` with a shadow `output()` that writes a sentinel marker; asserts no "shadowed resolution" warning on stderr per Spec 3.3. T-MOD-03b combines delegation + workflow-local precedence: builds a custom tmpdir with `projectRoot/package.json` (declares loopx), `projectRoot/node_modules/loopx` (project-root sentinel), `projectRoot/node_modules/.bin/loopx` (wrapper to real bin.js), `.loopx/ralph/node_modules/loopx/` (workflow-local sentinel), and `.loopx/ralph/index.ts` that imports and invokes `output()`; asserts the marker contains the workflow-local sentinel. T-MOD-03c [Node] creates `.loopx/ralph/package.json` with `{ type: "module" }`, `.loopx/ralph/lib/helper.js` with an ESM export, and `.loopx/ralph/index.ts` importing it; verifies Node's ESM resolution honors the workflow-level package.json `type` field. T-MOD-13h through T-MOD-13p test that `output()` does NOT validate goto shape at serialization time — the script exits 0, the first yielded `Output` preserves the raw goto value, and the generator throws at transition time. Current pass/fail against the pre-ADR-0003 src/ implementation: 13 pass, 67 fail (80 total). Passing tests (T-MOD-07, 08, 09, 13a, 13b, 13c × 2 runtimes, plus T-MOD-22) are crash/ESM-only tests whose expected outcome incidentally aligns with the pre-ADR-0003 "workflow not found" exit path. Once src/ migrates to ADR-0003, the remaining 67 should pass for the right reasons. Tests compile cleanly (`npx tsc --noEmit` passes; vitest reports "Type Errors: no errors"). Runtime ~24s.

### P1 — Cleanup

- [ ] **Delete tests/helpers/fixtures.ts `createScript`, `createDirScript`, and `createBashScript`** once all remaining legacy-using test files migrate. ADR-0003 §"Consequences" forbids legacy compatibility; these helpers must not persist once their last caller is converted. Remaining callers to migrate before the helpers can be removed: `tests/e2e/edge-cases.test.ts`, `tests/e2e/exit-codes.test.ts`, `tests/e2e/output-parsing.test.ts`, `tests/e2e/signals.test.ts`, `tests/harness/smoke.test.ts`, and the fuzz tests under `tests/fuzz/` (grep `createScript\|createDirScript\|createBashScript` to confirm the full set before deletion).
- [ ] **Audit 81 orphan test IDs** in tests/ that do not appear in TEST-SPEC.md — likely renumbered; decide rename vs delete.

### Runtime seam / internal test seams

- [ ] Confirm `LOOPX_TEST_INSTALL_FAULT=commit-fail-after:<n>` seam is tested once install tests land (TEST-SPEC §1.4).
- [ ] Verify `loopx/internal` barrel still exposes `parseOutput`, `parseEnvFile`, `classifySource` after refactor.

## Work-order strategy

1. Land workflow fixture helpers (P0 blockers) with no behavior change to existing tests — ship a commit that adds helpers without removing the old ones.
2. Incrementally migrate each e2e test file to workflow helpers in separate commits, one test file at a time. Each commit expected to introduce test failures against the un-updated src/ implementation (that is the intended design per PROMPT.md step 5).
3. Once all tests compile against the new helpers, delete `createScript` / `createDirScript` in one final cleanup commit.

## Notes

- Do NOT touch src/ in this phase. The test harness is the immediate deliverable. src/ migration to the workflow model is a separate follow-up (ADR-0003 is still Test Specified, not Implemented).
- `.loopx/.iteration.tmp` appears in `git status` — left by a prior loopx run, not part of this work. Not tracked.

---
"loop-extender": patch
---

Internal: migrate repo to a Turborepo monorepo with npm workspaces and Changesets-driven releases. No user-facing behavior changes — package continues to publish as `loop-extender` with the `loopx` bin.

---
"loop-extender": minor
---

Migrate repo to a Turborepo monorepo with npm workspaces and Changesets-driven releases. No user-facing behavior changes — package continues to publish as `loop-extender` with the `loopx` bin. Bumped to 0.2.0 to mark the new release infrastructure as a clean line in the version history.
